---
phase: 07-engagement-forums-and-faq
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - BlossomHubs/Features/Hubs/FAQ/FAQViewModel.swift
  - BlossomHubs/Features/Hubs/FAQ/FAQListView.swift
  - BlossomHubs/Features/Hubs/FAQ/FAQEntryRow.swift
  - BlossomHubs/Features/Hubs/Community/CommunitySectionPager.swift
  - BlossomHubs/BlossomHubs.xcodeproj/project.pbxproj
autonomous: true
requirements: [ENGR-04, ENGR-05, ENGR-06]

must_haves:
  truths:
    - "The FAQ section shows an accordion list of questions, with answered entries sorted first"
    - "Answered entries display a green checkmark and expand to show the answer with teal tint background and creator attribution"
    - "Unanswered entries show a clock icon, the asker name, and do not expand"
    - "An inline Ask text field at the top lets users with tier permission submit new questions"
    - "Users without FAQ permission see a disabled text field with an upgrade prompt naming the required tier"
    - "Creator answers use the same teal tint visual treatment as creator forum replies (ENGR-06 consistency)"
  artifacts:
    - path: "BlossomHubs/Features/Hubs/FAQ/FAQViewModel.swift"
      provides: "FAQ state management with sorted entries and question submission"
      exports: ["FAQViewModel"]
    - path: "BlossomHubs/Features/Hubs/FAQ/FAQListView.swift"
      provides: "Top-level FAQ view with inline ask field and accordion list"
      exports: ["FAQListView"]
    - path: "BlossomHubs/Features/Hubs/FAQ/FAQEntryRow.swift"
      provides: "Expandable FAQ row with answered/unanswered states"
      exports: ["FAQEntryRow"]
  key_links:
    - from: "CommunitySectionPager.swift"
      to: "FAQListView"
      via: "case .faq in sectionPage(for:)"
      pattern: "case \\.faq.*FAQListView"
    - from: "FAQListView"
      to: "FAQViewModel"
      via: "@State optional lazy init in .onAppear"
      pattern: "viewModel = FAQViewModel"
    - from: "FAQEntryRow"
      to: "BlossomTheme.teal.opacity(0.08)"
      via: "teal tint on answered entry expansion (ENGR-06 consistency)"
      pattern: "teal\\.opacity"
---

<objective>
Build the FAQ zone with accordion-style expandable entries, tier-gated question submission, and creator answer attribution -- replacing the FAQ EmptyStateView in CommunitySectionPager.

Purpose: Let subscribers browse answered FAQ entries and submit new questions (when their tier permits), with creator answers visually distinguished by the same teal tint treatment used in forum replies.
Output: 3 new Swift files (FAQViewModel, FAQListView, FAQEntryRow) plus CommunitySectionPager .faq wiring.
</objective>

<execution_context>
@/Users/nsimpson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nsimpson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-engagement-forums-and-faq/07-CONTEXT.md
@.planning/phases/07-engagement-forums-and-faq/07-RESEARCH.md
@.planning/phases/07-engagement-forums-and-faq/07-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from codebase and Plan 01. -->

From BlossomHubs/Models/Community.swift:
```swift
struct FAQEntry: Identifiable {
    let id: UUID
    let question: String
    let answer: String?
    let isAnswered: Bool
    let askedBy: String
    let answeredBy: String?
}

struct Community: Identifiable {
    let faqEntries: [FAQEntry]
    let tiers: [Tier]
    let creator: Creator
    // ...
}
```

From BlossomHubs/Core/Components/TagView.swift (updated in Plan 01):
```swift
enum TagStyle {
    case stock, tier, category, subscribed, role
    // .role: teal foreground on teal.opacity(0.12) background
}
```

From BlossomHubs/Features/Hubs/Community/CommunitySectionPager.swift (after Plan 01):
```swift
// .discussions now renders DiscussionsFeedView
// .faq still renders EmptyStateView -- replace in this plan
case .faq:
    EmptyStateView(icon: "questionmark.circle", title: "FAQ", subtitle: "FAQ coming in Phase 7")
```

Pattern reference -- ViewModel lazy init (Phase 3/6 established):
```swift
@State private var viewModel: SomeViewModel?
// In .onAppear: viewModel = SomeViewModel(community: community)
```

Pattern reference -- expandedEntryID (Phase 3 TiersBottomSheet):
```swift
@State private var expandedTierID: UUID?
// Toggle: expandedTierID = expandedTierID == id ? nil : id
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FAQViewModel, FAQEntryRow, and FAQListView with pager wiring</name>
  <files>
    BlossomHubs/Features/Hubs/FAQ/FAQViewModel.swift,
    BlossomHubs/Features/Hubs/FAQ/FAQEntryRow.swift,
    BlossomHubs/Features/Hubs/FAQ/FAQListView.swift,
    BlossomHubs/Features/Hubs/Community/CommunitySectionPager.swift,
    BlossomHubs/BlossomHubs.xcodeproj/project.pbxproj
  </files>
  <action>
    1. **FAQViewModel.swift** -- Create at `Features/Hubs/FAQ/FAQViewModel.swift`:
       - @MainActor @Observable final class
       - Properties: community (Community), entries (var [FAQEntry] -- mutable copy), faqRequiredTierIndex (Int, default 1 -- second tier threshold for question submission)
       - init(community:): copies community.faqEntries into entries
       - sortedEntries: computed [FAQEntry] that returns answered entries first (sorted by most recent), then unanswered entries (sorted by most recent). Since FAQEntry has no date field, maintain original order within each group -- answered first, unanswered second.
       - canAskQuestion(userTierIndex: Int?) -> Bool: guard let index else return false, return index >= faqRequiredTierIndex
       - requiredTierName: String computed from community.tiers[faqRequiredTierIndex].name (guard bounds)
       - submitQuestion(text:askedBy:): create new FAQEntry with isAnswered=false, append to entries array

    2. **FAQEntryRow.swift** -- Create at `Features/Hubs/FAQ/FAQEntryRow.swift`:
       - Takes: entry (FAQEntry), isExpanded (Bool), onToggle closure
       - Layout per CONTEXT.md and RESEARCH.md code example:
         - Question header (always visible): Button that calls onToggle containing HStack of:
           - Status icon: "checkmark.circle.fill" in BlossomTheme.teal for answered, "clock" in BlossomTheme.secondaryText for unanswered, font .system(size: 18)
           - Question text: BlossomFont.subhead, multilineTextAlignment(.leading), foregroundStyle primaryText
           - Spacer
           - Chevron (only for answered): "chevron.down" when expanded, "chevron.right" when collapsed, font .system(size: 14), secondaryText
         - Button .disabled(!entry.isAnswered) -- unanswered entries cannot expand
         - If isExpanded AND entry has answer: VStack with:
           - Answer text: BlossomFont.body, primaryText
           - Creator attribution: HStack of "checkmark.shield.fill" icon (.system(size: 12), teal) + "Answered by [answeredBy]" (BlossomFont.caption, secondaryText)
           - Entire answer section: padding 12, background BlossomTheme.teal.opacity(0.08), clipShape RoundedRectangle(cornerRadius: 8), padding(.top, 8)
         - If NOT answered: "Asked by [askedBy]" text below question (BlossomFont.caption, secondaryText, padding(.top, 4))
       - Outer VStack padding(.vertical, 12)

    3. **FAQListView.swift** -- Create at `Features/Hubs/FAQ/FAQListView.swift`:
       - Takes: community (Community)
       - @State private var viewModel: FAQViewModel? -- lazy init in .onAppear
       - @Environment(SubscriptionStore.self) private var subscriptionStore
       - @State private var expandedEntryID: UUID? -- single-expansion accordion (TiersBottomSheet pattern)
       - @State private var questionText = ""
       - Computes userTierIndex from subscriptionStore.currentTier(for: community.id) and community.tiers
       - NO ScrollView (participates in outer CommunityHubView scroll)
       - Body: VStack(alignment: .leading, spacing: 0) with:
         - **Inline Ask field** at top: HStack with TextField("Ask a question...", text: $questionText) and send Button (Image(systemName: "arrow.up.circle.fill"), violet color). TextField disabled when canAskQuestion returns false. Send button disabled when questionText.isEmpty or no permission.
         - If no permission: Text below the field "Upgrade to [requiredTierName] to ask questions" in BlossomFont.caption, orange color, padding(.bottom, 8). Tapping this text shows TiersBottomSheet via @State showUpgradeSheet bool.
         - On send: viewModel.submitQuestion(text: questionText, askedBy: "You"), clear questionText
         - Divider after ask field
         - ForEach over viewModel.sortedEntries rendering FAQEntryRow with:
           - isExpanded: expandedEntryID == entry.id
           - onToggle: withAnimation { expandedEntryID = expandedEntryID == entry.id ? nil : entry.id }
         - Divider between entries
       - .sheet for TiersBottomSheet upgrade prompt (same reuse pattern as ContentFeedView)
       - Spacer() at bottom

    4. **CommunitySectionPager.swift** -- Replace .faq placeholder:
       - Change `case .faq:` from EmptyStateView to `FAQListView(community: community)`

    5. **project.pbxproj** -- Register 3 new files under a "FAQ" group inside Features/Hubs:
       - FAQViewModel.swift, FAQEntryRow.swift, FAQListView.swift
       - Create the FAQ group in Xcode project structure
  </action>
  <verify>
    Build: xcodebuild build -project BlossomHubs/BlossomHubs.xcodeproj -scheme BlossomHubs -destination 'platform=iOS Simulator,name=iPhone 17 Pro' | tail -5
  </verify>
  <done>FAQ section shows accordion list with answered entries first (green checkmark) and unanswered entries below (clock icon), expandable answered entries show answer with teal tint + creator attribution, inline Ask field is tier-gated with upgrade prompt, CommunitySectionPager .faq case renders FAQListView</done>
</task>

</tasks>

<verification>
- Build succeeds with zero errors
- CommunitySectionPager .faq case renders FAQListView (not EmptyStateView)
- FAQ entries sorted: answered first, unanswered second
- Answered entries expand to show answer with teal background and creator attribution
- Ask field is disabled when user tier is below faqRequiredTierIndex
- Upgrade prompt names the required tier
- New questions appear in the unanswered section
</verification>

<success_criteria>
- FAQ zone visible to all subscribed users, question submission tier-gated (ENGR-04)
- Answered entries are persistent and discoverable with creator attribution (ENGR-05)
- Creator answers use teal tint background matching forum creator reply treatment (ENGR-06)
- Accordion expansion uses expandedEntryID UUID? pattern for single-expansion
</success_criteria>

<output>
After completion, create `.planning/phases/07-engagement-forums-and-faq/07-02-SUMMARY.md`
</output>
