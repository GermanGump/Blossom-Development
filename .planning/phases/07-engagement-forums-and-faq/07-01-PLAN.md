---
phase: 07-engagement-forums-and-faq
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BlossomHubs/Models/Community.swift
  - BlossomHubs/Core/Components/TagView.swift
  - BlossomHubs/Features/Hubs/Forums/ForumViewModel.swift
  - BlossomHubs/Features/Hubs/Forums/ForumThreadRow.swift
  - BlossomHubs/Features/Hubs/Forums/DiscussionsFeedView.swift
  - BlossomHubs/Features/Hubs/Forums/ForumThreadDetailView.swift
  - BlossomHubs/Features/Hubs/Forums/ForumReplyRow.swift
  - BlossomHubs/Features/Hubs/Forums/ForumComposeSheet.swift
  - BlossomHubs/Features/Hubs/Community/CommunitySectionPager.swift
  - BlossomHubs/BlossomHubs.xcodeproj/project.pbxproj
autonomous: true
requirements: [ENGR-01, ENGR-02, ENGR-03, ENGR-06]

must_haves:
  truths:
    - "The Discussions section shows compact forum thread rows with title, author, tier badge, reply count, like count, and timestamp"
    - "Locked threads (above user tier) show LockedContentOverlay with upgrade prompt naming the required tier"
    - "Tapping an accessible thread pushes ForumThreadDetailView with flat chronological replies"
    - "User can create a new thread via FAB compose sheet and the thread appears in the list"
    - "User can reply to a thread and the reply appears immediately in the flat list"
    - "User can tap heart to like/unlike threads and replies with immediate count update"
    - "Creator and ambassador replies have teal tint background and role badge pill"
    - "Every forum post and reply shows the poster tier badge via TagView(.tier)"
  artifacts:
    - path: "BlossomHubs/Models/Community.swift"
      provides: "ForumReply struct"
      contains: "struct ForumReply"
    - path: "BlossomHubs/Core/Components/TagView.swift"
      provides: "TagStyle.role case for creator/ambassador badge"
      contains: "case role"
    - path: "BlossomHubs/Features/Hubs/Forums/ForumViewModel.swift"
      provides: "Mutable thread/reply/like state management"
      exports: ["ForumViewModel"]
    - path: "BlossomHubs/Features/Hubs/Forums/DiscussionsFeedView.swift"
      provides: "Top-level view replacing EmptyStateView in pager"
      exports: ["DiscussionsFeedView"]
    - path: "BlossomHubs/Features/Hubs/Forums/ForumThreadDetailView.swift"
      provides: "Thread detail with flat reply list and reply input"
      exports: ["ForumThreadDetailView"]
  key_links:
    - from: "CommunitySectionPager.swift"
      to: "DiscussionsFeedView"
      via: "case .discussions in sectionPage(for:)"
      pattern: "case \\.discussions.*DiscussionsFeedView"
    - from: "DiscussionsFeedView"
      to: "ForumViewModel"
      via: "@State optional lazy init in .onAppear"
      pattern: "viewModel = ForumViewModel"
    - from: "ForumThreadRow"
      to: "ForumThreadDetailView"
      via: "NavigationLink push in existing Hubs NavigationStack"
      pattern: "NavigationLink"
    - from: "ForumReplyRow"
      to: "TagView(.role)"
      via: "creator/ambassador role badge rendering"
      pattern: "TagView.*\\.role"
---

<objective>
Build the complete discussion forums feature: thread list with tier-gated access, thread detail with flat reply list, thread creation via FAB compose sheet, like toggling, and creator/ambassador visual distinction with teal tint + role badge.

Purpose: Replace the Discussions EmptyStateView placeholder with a fully interactive forum that lets subscribers create threads, reply, and like -- with tier badges on every post and creator replies visually highlighted.
Output: 8 new/modified Swift files providing the complete forums experience wired into CommunitySectionPager.
</objective>

<execution_context>
@/Users/nsimpson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nsimpson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-engagement-forums-and-faq/07-CONTEXT.md
@.planning/phases/07-engagement-forums-and-faq/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From BlossomHubs/Models/Community.swift:
```swift
struct ForumThread: Identifiable {
    let id: UUID
    let title: String
    let content: String
    let authorId: UUID
    let requiredTierIndex: Int
    let replyCount: Int
    let likeCount: Int
    let publishedAt: Date
}

struct Community: Identifiable {
    let creator: Creator
    let tiers: [Tier]
    let threads: [ForumThread]
    // ...
}

struct Creator: Identifiable {
    let id: UUID
    let name: String
    let profileImageName: String
    let isVerified: Bool
    let isAmbassador: Bool
}
```

From BlossomHubs/Core/Components/TagView.swift:
```swift
enum TagStyle {
    case stock, tier, category, subscribed
    // Add case role here
}

struct TagView: View {
    init(_ text: String, style: TagStyle)
}
```

From BlossomHubs/Features/Hubs/Feed/ContentFeedViewModel.swift:
```swift
// Pattern to follow for ForumViewModel:
@MainActor @Observable
final class ContentFeedViewModel {
    let community: Community
    init(community: Community, filterToVideos: Bool = false)
}
```

From BlossomHubs/Features/Hubs/Community/CommunitySectionPager.swift:
```swift
// Replace .discussions EmptyStateView:
case .discussions:
    EmptyStateView(icon: "bubble.left.and.bubble.right", title: "Discussions", subtitle: "Discussions coming in Phase 7")
```

From BlossomHubs/Features/Hubs/HubsNavigation.swift:
```swift
enum HubsRoute: Hashable {
    case communityDetail(id: String)
    case communityPreview(id: String)
    case mySubscriptions
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ForumReply model, TagStyle.role, ForumViewModel, and thread list views</name>
  <files>
    BlossomHubs/Models/Community.swift,
    BlossomHubs/Core/Components/TagView.swift,
    BlossomHubs/Features/Hubs/Forums/ForumViewModel.swift,
    BlossomHubs/Features/Hubs/Forums/ForumThreadRow.swift,
    BlossomHubs/Features/Hubs/Forums/DiscussionsFeedView.swift
  </files>
  <action>
    1. **Community.swift** -- Add ForumReply struct after the ForumThread definition:
       - Properties: id (UUID), threadID (UUID), authorId (UUID), authorName (String), authorTierName (String), content (String), likeCount (Int), publishedAt (Date), isCreator (Bool), isAmbassador (Bool)
       - All properties are `let` with default init (id = UUID(), likeCount = 0, isCreator = false, isAmbassador = false)
       - Conforms to Identifiable

    2. **TagView.swift** -- Add `case role` to TagStyle enum:
       - foregroundColor: `BlossomTheme.teal`
       - backgroundColor: `BlossomTheme.teal.opacity(0.12)`
       - Update the preview to include TagView("Creator", style: .role)

    3. **ForumViewModel.swift** -- Create at `Features/Hubs/Forums/ForumViewModel.swift`:
       - @MainActor @Observable final class
       - Properties: community (Community), threads (var [ForumThread] -- mutable copy), likedThreadIDs (Set of UUID), likedReplyIDs (Set of UUID), replies ([UUID: [ForumReply]] dictionary)
       - init(community:) copies community.threads sorted by publishedAt descending, then generates 3-5 mock ForumReply objects per accessible thread including at least one reply with isCreator=true per thread (use community.creator.name). Use realistic member names (e.g., "TraderMike", "InvestorJane", "CryptoCarl").
       - canAccessThread(_ thread: ForumThread, userTierIndex: Int?) -> Bool: guard let index else return false, return thread.requiredTierIndex <= index
       - tierName(for requiredIndex: Int) -> String: same pattern as ContentFeedViewModel
       - toggleThreadLike(threadID:): insert/remove from likedThreadIDs set
       - toggleReplyLike(replyID:): insert/remove from likedReplyIDs set
       - addThread(title:content:authorTierName:): create new ForumThread, prepend to threads array
       - addReply(threadID:authorName:authorTierName:content:): create ForumReply, append to replies[threadID], also increment the corresponding thread's replyCount -- since ForumThread uses let, replace the thread in the array with a new instance that has replyCount + 1

    4. **ForumThreadRow.swift** -- Create compact list row view:
       - Takes: thread (ForumThread), authorName (String), authorProfileImage (String), isCreator (Bool), isAmbassador (Bool), tierName (String), isLiked (Bool), onLike closure
       - Layout: HStack with AvatarView(.small), VStack of title (BlossomFont.subhead, 2 line limit) and HStack of author name + TagView(tierName, .tier) + optional TagView("Creator"/"Ambassador", .role), then Spacer, then VStack of reply count icon + like heart
       - Heart icon: "heart.fill" red when liked, "heart" secondaryText when not
       - Reply count: "bubble.right" icon + count
       - Relative timestamp using .formatted(.relative(presentation: .named))
       - If isCreator or isAmbassador: apply teal tint background BlossomTheme.teal.opacity(0.08)
       - Divider below each row

    5. **DiscussionsFeedView.swift** -- Create top-level Discussions section view:
       - Takes: community (Community)
       - @State private var viewModel: ForumViewModel? -- lazy init in .onAppear (established pattern)
       - @Environment(SubscriptionStore.self) private var subscriptionStore
       - @State private var showComposeSheet = false
       - Computes userTierIndex from subscriptionStore.currentTier(for: community.id) and community.tiers
       - NO ScrollView (participates in outer CommunityHubView scroll)
       - Body: VStack(alignment: .leading, spacing: 0) with ForEach over viewModel.threads
       - Each thread: if canAccessThread -> ForumThreadRow wrapped in NavigationLink that pushes ForumThreadDetailView; if locked -> ForumThreadRow overlaid with LockedContentOverlay and tap shows TiersBottomSheet
       - FAB overlay: bottom-trailing "+" button (circle, violet background, white plus icon) visible only when user has forum access (userTierIndex != nil). Uses .overlay(alignment: .bottomTrailing) with padding
       - FAB taps set showComposeSheet = true
       - .sheet(isPresented: $showComposeSheet) presents ForumComposeSheet
       - Thread author resolution: compare thread.authorId == community.creator.id to determine isCreator, use community.creator.isAmbassador for isAmbassador, use community.creator.name/profileImageName for creator threads (for non-creator threads use generic "Member" name and "person.circle" SF Symbol)
  </action>
  <verify>
    Build: xcodebuild build -project BlossomHubs/BlossomHubs.xcodeproj -scheme BlossomHubs -destination 'platform=iOS Simulator,name=iPhone 17 Pro' | tail -5
  </verify>
  <done>ForumReply model exists, TagStyle.role renders teal pills, ForumViewModel manages mutable forum state with mock replies, ForumThreadRow displays compact rows with tier badges and creator highlights, DiscussionsFeedView renders thread list with FAB and tier gating</done>
</task>

<task type="auto">
  <name>Task 2: Thread detail, reply row, compose sheet, and pager wiring</name>
  <files>
    BlossomHubs/Features/Hubs/Forums/ForumThreadDetailView.swift,
    BlossomHubs/Features/Hubs/Forums/ForumReplyRow.swift,
    BlossomHubs/Features/Hubs/Forums/ForumComposeSheet.swift,
    BlossomHubs/Features/Hubs/Community/CommunitySectionPager.swift,
    BlossomHubs/BlossomHubs.xcodeproj/project.pbxproj
  </files>
  <action>
    1. **ForumReplyRow.swift** -- Individual reply view used in thread detail:
       - Takes: reply (ForumReply), isLiked (Bool), onLike closure
       - Layout per CONTEXT.md visual stack:
         - HStack: AvatarView (use "person.circle" SF Symbol for non-creator, community creator profileImageName for creator/ambassador replies -- pass profileImage as parameter), author name (BlossomFont.subhead), TagView(reply.authorTierName, .tier), optional TagView("Creator"/.role) or TagView("Ambassador", .role), Spacer, relative timestamp
         - Text(reply.content) with BlossomFont.body
         - Like button: HStack of heart icon + count
       - If reply.isCreator || reply.isAmbassador: entire row gets BlossomTheme.teal.opacity(0.08) background + .clipShape(RoundedRectangle(cornerRadius: 8))
       - AvatarView showVerifiedBadge: reply.isCreator || reply.isAmbassador
       - Padding 12 all sides

    2. **ForumThreadDetailView.swift** -- Pushed via NavigationLink from DiscussionsFeedView:
       - Takes: community (Community), thread (ForumThread), viewModel (ForumViewModel)
       - This IS a separate screen so it CAN have its own ScrollView (exception to no-ScrollView rule)
       - @State private var replyText = ""
       - @Environment(SubscriptionStore.self) private var subscriptionStore
       - Layout: VStack with ScrollView containing:
         - Original post at top: author row (AvatarView + name + tier badge + role badge if creator + timestamp), thread title (BlossomFont.headline), thread content (BlossomFont.body), like button row
         - Divider
         - "Replies" section header with count
         - ForEach over viewModel.replies[thread.id] rendering ForumReplyRow for each
       - Bottom: HStack with TextField("Write a reply...", text: $replyText) and Send button (Image(systemName: "arrow.up.circle.fill") in violet). Send button disabled when replyText is empty. On send: call viewModel.addReply with current user tier name, clear replyText
       - Bottom input field pinned outside ScrollView using VStack { ScrollView { ... } inputBar }
       - .navigationTitle(thread.title) with .navigationBarTitleDisplayMode(.inline)
       - Resolve poster identity same as DiscussionsFeedView (compare authorId to creator.id)

    3. **ForumComposeSheet.swift** -- Sheet for creating new thread:
       - @State private var title = ""
       - @State private var content = ""
       - @Environment(\.dismiss) private var dismiss
       - Takes: viewModel (ForumViewModel), userTierName (String)
       - Layout: NavigationStack wrapping Form with Section for title TextField and content TextField (axis: .vertical, lineLimit 3...8)
       - .navigationTitle("New Thread")
       - Toolbar: leading Cancel button (dismiss), trailing Post button (calls viewModel.addThread, then dismiss). Post disabled when title or content is empty.

    4. **CommunitySectionPager.swift** -- Replace .discussions placeholder:
       - Change `case .discussions:` from EmptyStateView to `DiscussionsFeedView(community: community)`
       - Keep .faq as EmptyStateView (Plan 02 replaces it)

    5. **project.pbxproj** -- Register all 6 new files under a "Forums" group inside Features/Hubs:
       - ForumViewModel.swift, ForumThreadRow.swift, DiscussionsFeedView.swift, ForumThreadDetailView.swift, ForumReplyRow.swift, ForumComposeSheet.swift
       - Create the Forums group in the Xcode project structure
  </action>
  <verify>
    Build: xcodebuild build -project BlossomHubs/BlossomHubs.xcodeproj -scheme BlossomHubs -destination 'platform=iOS Simulator,name=iPhone 17 Pro' | tail -5
  </verify>
  <done>ForumThreadDetailView pushes from thread rows with flat reply list and reply input, ForumReplyRow renders replies with teal creator highlights, ForumComposeSheet creates new threads, CommunitySectionPager .discussions case renders DiscussionsFeedView, all files registered in Xcode project</done>
</task>

</tasks>

<verification>
- Build succeeds with zero errors
- CommunitySectionPager .discussions case renders DiscussionsFeedView (not EmptyStateView)
- Forum thread rows display with tier badges, like counts, reply counts
- Creator/ambassador replies show teal background + role badge pill
- FAB visible only when user has forum access
- Thread detail pushes onto existing NavigationStack
</verification>

<success_criteria>
- Forums section shows compact thread list rows with tier badges (ENGR-01, ENGR-03)
- Locked threads show LockedContentOverlay with required tier name (ENGR-01)
- User can create thread, reply, and like (ENGR-02)
- Creator/ambassador replies have teal tint + role badge in both list and detail (ENGR-06)
- All interactions update in-memory state immediately (ENGR-02)
</success_criteria>

<output>
After completion, create `.planning/phases/07-engagement-forums-and-faq/07-01-SUMMARY.md`
</output>
