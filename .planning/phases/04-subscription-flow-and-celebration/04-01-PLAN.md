---
phase: 04-subscription-flow-and-celebration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BlossomHubs/Models/Subscription.swift
  - BlossomHubs/Models/SubscriptionStore.swift
  - BlossomHubs/Features/Hubs/Payment/MockPaymentSheetView.swift
  - BlossomHubs/Features/Hubs/Payment/PaymentViewModel.swift
  - BlossomHubs/Features/Hubs/Preview/TierCardView.swift
  - BlossomHubs/Features/Hubs/Preview/TiersBottomSheet.swift
  - BlossomHubs/Features/Hubs/Preview/CommunityPreviewView.swift
  - BlossomHubs/App/BlossomHubsApp.swift
  - BlossomHubs/App/ContentView.swift
  - BlossomHubs/BlossomHubs.xcodeproj/project.pbxproj
autonomous: true
requirements: [SUBS-04, SUBS-05]

must_haves:
  truths:
    - "SubscriptionStore is injected into the environment and accessible from any view"
    - "Tapping Subscribe on a tier card presents a Stripe-styled payment sheet stacked on top of the tier sheet"
    - "Payment sheet shows card number, expiry, CVC fields with visual formatting and card brand detection"
    - "Tapping Pay shows a Processing spinner for 1-2 seconds then transitions to success state"
    - "Payment always succeeds regardless of input — no error/decline flow"
  artifacts:
    - path: "BlossomHubs/Models/Subscription.swift"
      provides: "Subscription model + UserSession struct"
      contains: "struct Subscription"
    - path: "BlossomHubs/Models/SubscriptionStore.swift"
      provides: "@MainActor @Observable subscription state with UserDefaults persistence"
      contains: "class SubscriptionStore"
    - path: "BlossomHubs/Features/Hubs/Payment/MockPaymentSheetView.swift"
      provides: "Stripe-styled mock payment form"
      contains: "struct MockPaymentSheetView"
    - path: "BlossomHubs/Features/Hubs/Payment/PaymentViewModel.swift"
      provides: "Payment state machine (idle -> processing -> success)"
      contains: "enum PaymentState"
  key_links:
    - from: "BlossomHubs/Features/Hubs/Preview/TierCardView.swift"
      to: "MockPaymentSheetView"
      via: "onSubscribe callback triggers sheet presentation in TiersBottomSheet"
      pattern: "onSubscribe.*tier"
    - from: "BlossomHubs/App/BlossomHubsApp.swift"
      to: "SubscriptionStore"
      via: ".environment(subscriptionStore)"
      pattern: "\\.environment.*subscriptionStore"
    - from: "MockPaymentSheetView"
      to: "PaymentViewModel"
      via: "submitPayment() async call on Pay tap"
      pattern: "submitPayment"
---

<objective>
Create the subscription data layer and mock Stripe payment sheet — the SubscriptionStore that tracks which communities a user has subscribed to (persisted via UserDefaults), the Subscription/UserSession models, a Stripe-accurate payment sheet with card formatting and brand detection, and a PaymentViewModel state machine that drives the idle -> processing -> success flow.

Purpose: Establishes the subscription state foundation that every subsequent plan in this phase depends on, and delivers the first visible user interaction: tapping Subscribe and seeing a realistic payment form.
Output: SubscriptionStore injected into environment, mock payment sheet presenting from tier cards, payment processing simulation.
</objective>

<execution_context>
@/Users/nsimpson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nsimpson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-subscription-flow-and-celebration/04-RESEARCH.md

@BlossomHubs/Features/Hubs/Preview/TierCardView.swift
@BlossomHubs/Features/Hubs/Preview/TiersBottomSheet.swift
@BlossomHubs/Features/Hubs/Preview/CommunityPreviewView.swift
@BlossomHubs/App/BlossomHubsApp.swift
@BlossomHubs/App/ContentView.swift
@BlossomHubs/Models/Community.swift

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From BlossomHubs/Models/Community.swift:
```swift
struct Tier: Identifiable {
    let id: UUID
    let name: String
    let monthlyPrice: Decimal
    let benefits: [String]
    // ... other fields
}

struct Community: Identifiable {
    let id: UUID
    let name: String
    var tiers: [Tier]
    // ... other fields
}
```

From BlossomHubs/Features/Hubs/Preview/TierCardView.swift:
```swift
struct TierCardView: View {
    let tier: Tier
    var isPopular: Bool = false
    let isExpanded: Bool
    let onTap: () -> Void
    // Subscribe button at line 66-68: Button("Subscribe") { // Phase 4 wires this }
}
```

From BlossomHubs/Features/Hubs/Preview/TiersBottomSheet.swift:
```swift
struct TiersBottomSheet: View {
    let community: Community
    let tiers: [Tier]
    let popularTierIndex: Int
    @State private var expandedTierID: UUID? = nil
}
```

From BlossomHubs/Features/Hubs/Preview/CommunityPreviewView.swift:
```swift
struct CommunityPreviewView: View {
    let communityID: String
    @State private var showTiers = false
    // .sheet(isPresented: $showTiers) presents TiersBottomSheet
}
```

From BlossomHubs/App/BlossomHubsApp.swift:
```swift
struct BlossomHubsApp: App {
    @State private var store = CommunityStore()
    // ContentView().environment(store)
}
```

From BlossomHubs/Core/Components/TagView.swift:
```swift
enum TagStyle {
    case stock, tier, category
    // Phase 4 adds: case subscribed
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Subscription model, SubscriptionStore, and environment wiring</name>
  <files>
    BlossomHubs/Models/Subscription.swift,
    BlossomHubs/Models/SubscriptionStore.swift,
    BlossomHubs/App/BlossomHubsApp.swift,
    BlossomHubs/App/ContentView.swift,
    BlossomHubs/BlossomHubs.xcodeproj/project.pbxproj
  </files>
  <action>
    Create BlossomHubs/Models/Subscription.swift with:
    - `struct Subscription: Codable, Identifiable` — id (UUID), communityID (UUID), tierID (UUID), tierName (String), monthlyPrice (Decimal), subscribedAt (Date). Default id = UUID(), default subscribedAt = .now.
    - `struct UserSession: Codable` — name (String), username (String), profileImageName (String), subscriptions ([UUID: Subscription] keyed by communityID).

    Create BlossomHubs/Models/SubscriptionStore.swift with:
    - `@MainActor @Observable final class SubscriptionStore` following the exact pattern from 04-RESEARCH.md Pattern 1.
    - Storage key: "blossom_user_session"
    - Default mock user: name "Alex", username "@alex_investor", profileImageName "alex-profile", empty subscriptions.
    - Methods: subscribe(to:tier:), changeTier(for:to:), cancel(communityID:), isSubscribed(to:) -> Bool, currentTier(for:) -> UUID?
    - `#if DEBUG func resetAll()` clears all subscriptions and persists.
    - Private persist() encodes session to UserDefaults via JSONEncoder.
    - Init loads from UserDefaults via JSONDecoder, falls back to default session.

    Modify BlossomHubsApp.swift:
    - Add `@State private var subscriptionStore = SubscriptionStore()` alongside existing CommunityStore.
    - Chain `.environment(subscriptionStore)` after existing `.environment(store)`.

    Register both new Swift files in project.pbxproj under BlossomHubs/Models/ group (filename-only paths per Phase 3 decision about nested group PBXFileReference paths).
  </action>
  <verify>
    <automated>xcodebuild build -scheme BlossomHubs -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5</automated>
  </verify>
  <done>SubscriptionStore compiles with all methods, is injected into environment via BlossomHubsApp, and the project builds cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Mock Stripe payment sheet, PaymentViewModel, and tier card wiring</name>
  <files>
    BlossomHubs/Features/Hubs/Payment/MockPaymentSheetView.swift,
    BlossomHubs/Features/Hubs/Payment/PaymentViewModel.swift,
    BlossomHubs/Features/Hubs/Preview/TierCardView.swift,
    BlossomHubs/Features/Hubs/Preview/TiersBottomSheet.swift,
    BlossomHubs/BlossomHubs.xcodeproj/project.pbxproj
  </files>
  <action>
    Create BlossomHubs/Features/Hubs/Payment/PaymentViewModel.swift:
    - `enum PaymentState: Equatable { case idle, processing, success }`
    - `@MainActor @Observable final class PaymentViewModel` with state (PaymentState = .idle), community (Community), tier (Tier).
    - `func submitPayment() async` — sets state = .processing, does Task.sleep(for: .seconds(Double.random(in: 1.0...2.0))), sets state = .success.
    - `var isProcessing: Bool { state == .processing }`

    Create BlossomHubs/Features/Hubs/Payment/MockPaymentSheetView.swift:
    - Stripe-accurate design per locked decisions: dark input field backgrounds (Color(hex: "1A1F36") or similar dark navy), white text, rounded corners.
    - Top section: tier summary showing tier.name, formatted price, abbreviated benefits list.
    - Card number field: @State cardNumber String, on change runs formatCardNumber() helper that strips non-digits, limits to 16, inserts space every 4 chars. Display card brand icon (SF Symbol): first digit 4 = "creditcard.fill" label "Visa", 5 = "creditcard.fill" label "Mastercard", 3 = "creditcard.fill" label "Amex". Show brand text next to field.
    - Expiry field: @State expiry String, auto-format MM/YY with "/" inserted after 2 digits. Limit to 4 digits.
    - CVC field: @State cvc String, limit to 3-4 digits.
    - "Powered by Stripe" text + SF Symbol "creditcard" at bottom of sheet.
    - "Pay $X.XX" button using BlossomPrimaryButton style. When tapped and state == .idle, call `Task { await viewModel.submitPayment() }`.
    - When state == .processing: button shows ProgressView() + "Processing..." text, all fields disabled, interactiveDismissDisabled(true).
    - When state == .success: call onSuccess callback closure, dismiss the sheet.
    - Accept `onSuccess: () -> Void` callback and `@Environment(\.dismiss)` for sheet dismissal.
    - Apply `.sensoryFeedback(.success, trigger: viewModel.state) { _, new in new == .success }` for haptic on payment success.
    - Use `.presentationDetents([.large])` and `.interactiveDismissDisabled(viewModel.isProcessing)`.

    Modify TierCardView.swift:
    - Add `var onSubscribe: ((Tier) -> Void)? = nil` optional callback parameter (keeps backward compatibility with previews).
    - Replace `Button("Subscribe") { // Phase 4 wires this }` with `Button("Subscribe") { onSubscribe?(tier) }`.

    Modify TiersBottomSheet.swift:
    - Add `@State private var tierForPayment: Tier? = nil` to track which tier triggered payment.
    - Add `@Environment(SubscriptionStore.self) private var subscriptionStore` to access subscription state.
    - Pass `onSubscribe: { tier in tierForPayment = tier }` to each TierCardView.
    - Add `.sheet(item: $tierForPayment)` presenting MockPaymentSheetView with community and selected tier.
    - In onSuccess callback: call `subscriptionStore.subscribe(to: community, tier: selectedTier)`, dismiss the payment sheet, then dismiss the tier sheet. Use @Environment(\.dismiss) carefully — the payment sheet's onSuccess should set tierForPayment = nil (dismisses payment sheet), then after a brief delay set a binding or callback to dismiss the parent tier sheet.

    Create Features/Hubs/Payment/ group in project.pbxproj and register both new files (filename-only paths).

    IMPORTANT: Tier must conform to Identifiable for .sheet(item:) to work — it already does. Tier also needs Hashable conformance for the sheet item binding; if not present, add it.
  </action>
  <verify>
    <automated>xcodebuild build -scheme BlossomHubs -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5</automated>
  </verify>
  <done>Tapping Subscribe on any tier card in the bottom sheet presents a Stripe-styled payment form. Tapping Pay shows a spinner for 1-2 seconds, then triggers success with haptic feedback. The subscription is recorded in SubscriptionStore.</done>
</task>

</tasks>

<verification>
- Project builds with zero errors and zero Swift 6 strict concurrency warnings
- SubscriptionStore accessible via @Environment in any child view
- Payment sheet presents as stacked sheet on top of tier bottom sheet
- Card number auto-formats with spaces every 4 digits
- Card brand icon updates based on first digit
- Pay button transitions: idle (enabled) -> processing (spinner, disabled) -> success (dismisses)
- Haptic feedback fires on payment success
</verification>

<success_criteria>
- Mock Stripe payment sheet presents from any tier's Subscribe button with card/expiry/CVC fields (SUBS-04)
- Payment processing shows loading spinner then success after 1-2 seconds (SUBS-05)
- Subscription recorded in SubscriptionStore and persisted to UserDefaults
- No Swift 6 concurrency warnings
</success_criteria>

<output>
After completion, create `.planning/phases/04-subscription-flow-and-celebration/04-01-SUMMARY.md`
</output>
