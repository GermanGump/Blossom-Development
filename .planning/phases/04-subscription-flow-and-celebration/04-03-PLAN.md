---
phase: 04-subscription-flow-and-celebration
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - BlossomHubs/Features/Hubs/Preview/TierCardView.swift
  - BlossomHubs/Features/Hubs/Preview/TiersBottomSheet.swift
  - BlossomHubs/Features/Hubs/Preview/CommunityPreviewView.swift
  - BlossomHubs/Features/Hubs/Subscription/MySubscriptionsView.swift
  - BlossomHubs/Features/Hubs/Subscription/CancelRetentionSheet.swift
  - BlossomHubs/Features/Hubs/HubsNavigation.swift
  - BlossomHubs/Features/Hubs/HubsView.swift
  - BlossomHubs/BlossomHubs.xcodeproj/project.pbxproj
autonomous: true
requirements: [SUBS-08]

must_haves:
  truths:
    - "Subscribed community preview CTA shows Your Subscription instead of View Tiers"
    - "Tier sheet shows current tier with Current Plan label when user is subscribed"
    - "Other tiers show Upgrade or Downgrade buttons instead of Subscribe"
    - "Upgrade/downgrade triggers alert dialog with tier name and price"
    - "Cancel flow shows retention bottom sheet asking why before confirming"
    - "After canceling, community returns to pre-subscribed state with View Tiers CTA"
    - "My Subscriptions screen shows all active subscriptions across communities"
    - "Debug reset toggle clears all subscriptions"
  artifacts:
    - path: "BlossomHubs/Features/Hubs/Subscription/MySubscriptionsView.swift"
      provides: "Full subscription management list screen"
      contains: "struct MySubscriptionsView"
    - path: "BlossomHubs/Features/Hubs/Subscription/CancelRetentionSheet.swift"
      provides: "Retention bottom sheet with reason selection"
      contains: "struct CancelRetentionSheet"
    - path: "BlossomHubs/Features/Hubs/HubsNavigation.swift"
      provides: "HubsRoute.mySubscriptions case"
      contains: "case mySubscriptions"
  key_links:
    - from: "TiersBottomSheet"
      to: "SubscriptionStore"
      via: "Dual-mode: browse (Subscribe buttons) vs manage (Upgrade/Downgrade/Cancel buttons)"
      pattern: "isSubscribed"
    - from: "CommunityPreviewView"
      to: "SubscriptionStore"
      via: "CTA text changes: View Tiers vs Your Subscription"
      pattern: "isSubscribed.*communityID"
    - from: "MySubscriptionsView"
      to: "SubscriptionStore"
      via: "Lists session.subscriptions values"
      pattern: "subscriptionStore.*session.*subscriptions"
---

<objective>
Build subscription lifecycle management — upgrade, downgrade, and cancel flows within the tier sheet, a dedicated My Subscriptions screen, and a retention bottom sheet for cancellation. Transform the tier sheet into a dual-mode view (browse vs manage) and update the preview CTA based on subscription state.

Purpose: Completes the subscription lifecycle so a user can manage their subscription after subscribing, making the demo feel like a real product.
Output: Dual-mode tier sheet, My Subscriptions screen, cancel retention flow, debug reset toggle.
</objective>

<execution_context>
@/Users/nsimpson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nsimpson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-subscription-flow-and-celebration/04-RESEARCH.md
@.planning/phases/04-subscription-flow-and-celebration/04-01-SUMMARY.md
@.planning/phases/04-subscription-flow-and-celebration/04-02-SUMMARY.md

@BlossomHubs/Features/Hubs/Preview/TierCardView.swift
@BlossomHubs/Features/Hubs/Preview/TiersBottomSheet.swift
@BlossomHubs/Features/Hubs/Preview/CommunityPreviewView.swift
@BlossomHubs/Features/Hubs/HubsNavigation.swift
@BlossomHubs/Features/Hubs/HubsView.swift
@BlossomHubs/Models/SubscriptionStore.swift
@BlossomHubs/Models/Subscription.swift
@BlossomHubs/Models/Community.swift

<interfaces>
<!-- Key contracts from Plans 01-02 that this plan depends on. -->

From BlossomHubs/Models/SubscriptionStore.swift (created in Plan 01):
```swift
@MainActor @Observable final class SubscriptionStore {
    var session: UserSession
    func subscribe(to community: Community, tier: Tier)
    func changeTier(for communityID: UUID, to tier: Tier)
    func cancel(communityID: UUID)
    func isSubscribed(to communityID: UUID) -> Bool
    func currentTier(for communityID: UUID) -> UUID?
    #if DEBUG func resetAll() #endif
}
```

From BlossomHubs/Models/Subscription.swift (created in Plan 01):
```swift
struct Subscription: Codable, Identifiable {
    let id: UUID
    let communityID: UUID
    let tierID: UUID
    let tierName: String
    let monthlyPrice: Decimal
    let subscribedAt: Date
}

struct UserSession: Codable {
    let name: String
    let username: String
    let profileImageName: String
    var subscriptions: [UUID: Subscription]
}
```

From BlossomHubs/Features/Hubs/HubsNavigation.swift:
```swift
enum HubsRoute: Hashable {
    case communityDetail(id: String)
    case communityPreview(id: String)
    // Plan 03 adds: case mySubscriptions
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dual-mode tier sheet with upgrade/downgrade/cancel and retention flow</name>
  <files>
    BlossomHubs/Features/Hubs/Preview/TierCardView.swift,
    BlossomHubs/Features/Hubs/Preview/TiersBottomSheet.swift,
    BlossomHubs/Features/Hubs/Preview/CommunityPreviewView.swift,
    BlossomHubs/Features/Hubs/Subscription/CancelRetentionSheet.swift,
    BlossomHubs/BlossomHubs.xcodeproj/project.pbxproj
  </files>
  <action>
    Modify TierCardView.swift to support dual modes:
    - Add optional parameters: `var isCurrentPlan: Bool = false`, `var subscriptionAction: SubscriptionAction? = nil`.
    - Define `enum SubscriptionAction { case upgrade, downgrade }` (can be in TierCardView or a shared location).
    - When isCurrentPlan == true: show "Current Plan" label (teal text, BlossomFont.caption) next to tier name instead of "Most Popular" tag. Subscribe button hidden.
    - When subscriptionAction == .upgrade: button text "Upgrade", uses BlossomPrimaryButton.
    - When subscriptionAction == .downgrade: button text "Downgrade", uses BlossomSecondaryButton (outlined style).
    - Add `var onSubscriptionAction: ((Tier, SubscriptionAction) -> Void)? = nil` callback for upgrade/downgrade taps.
    - Keep existing onSubscribe callback for browse mode (non-subscribed state).

    Modify TiersBottomSheet.swift for dual-mode behavior:
    - Read subscription state: `@Environment(SubscriptionStore.self) private var subscriptionStore`.
    - Determine mode: if subscriptionStore.isSubscribed(to: community.id) -> "manage" mode, else "browse" mode.
    - In manage mode: header shows "Your Subscription" (not "Choose a Tier").
    - For each tier card, determine: is this the current tier? (subscriptionStore.currentTier == tier.id). Is it higher or lower than current? Compare tier index in community.tiers array — higher index = upgrade, lower index = downgrade.
    - Pass isCurrentPlan, subscriptionAction, onSubscriptionAction to each TierCardView.
    - On upgrade/downgrade tap: present an alert using `.alert()` modifier: "Upgrade to [Tier] for $X/mo?" or "Downgrade to [Tier] for $X/mo?" with Confirm and Cancel buttons.
    - On Confirm: call subscriptionStore.changeTier(for: community.id, to: selectedTier). Show brief haptic feedback (.success).
    - Add a "Cancel Subscription" button at the bottom of manage mode (BlossomGhostButton style, red text).
    - Add `@State private var showCancelRetention = false` and `@State private var showChangeTierAlert = false` with `@State private var pendingTierChange: (tier: Tier, action: SubscriptionAction)?`.

    Create BlossomHubs/Features/Hubs/Subscription/CancelRetentionSheet.swift:
    - Bottom sheet asking "Why are you leaving?" with selectable reasons:
      - "Too expensive"
      - "Not enough content"
      - "Found a better alternative"
      - "Just taking a break"
      - "Other"
    - Each reason is a tappable row with radio-button style selection (circle outline, filled when selected).
    - @State selectedReason: String? = nil.
    - "Cancel Subscription" button (red, destructive style) enabled only when a reason is selected.
    - "Never mind, keep subscription" secondary button that dismisses.
    - On cancel confirm: calls onConfirmCancel callback closure.
    - Use .presentationDetents([.medium]).

    Wire cancel retention in TiersBottomSheet:
    - "Cancel Subscription" button tap sets showCancelRetention = true.
    - .sheet(isPresented: $showCancelRetention) presents CancelRetentionSheet.
    - CancelRetentionSheet's onConfirmCancel: subscriptionStore.cancel(communityID: community.id), dismiss retention sheet, dismiss tier sheet.

    Modify CommunityPreviewView.swift:
    - The sticky CTA text changes based on subscription state:
      - Not subscribed: "View Tiers" (current behavior)
      - Subscribed: "Your Subscription"
    - Add `@Environment(SubscriptionStore.self) private var subscriptionStore`.
    - Check `subscriptionStore.isSubscribed(to: community.id)` for CTA text.

    Register CancelRetentionSheet.swift in project.pbxproj under new Features/Hubs/Subscription/ group.
  </action>
  <verify>
    <automated>xcodebuild build -scheme BlossomHubs -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5</automated>
  </verify>
  <done>Subscribed user sees "Your Subscription" CTA. Tier sheet shows current plan labeled, other tiers show Upgrade/Downgrade. Alert confirms tier change. Cancel shows retention sheet with reason selection. After canceling, community returns to pre-subscribed state.</done>
</task>

<task type="auto">
  <name>Task 2: My Subscriptions screen with navigation routing and debug reset</name>
  <files>
    BlossomHubs/Features/Hubs/Subscription/MySubscriptionsView.swift,
    BlossomHubs/Features/Hubs/HubsNavigation.swift,
    BlossomHubs/Features/Hubs/HubsView.swift,
    BlossomHubs/BlossomHubs.xcodeproj/project.pbxproj
  </files>
  <action>
    Add `case mySubscriptions` to HubsRoute enum in HubsNavigation.swift.

    Create BlossomHubs/Features/Hubs/Subscription/MySubscriptionsView.swift:
    - @Environment(SubscriptionStore.self) private var subscriptionStore
    - @Environment(CommunityStore.self) private var communityStore (to look up community details by ID)
    - Header: "My Subscriptions" (BlossomFont.title)
    - If no subscriptions: EmptyStateView with message "No active subscriptions yet. Browse communities to get started!" (use existing EmptyStateView component).
    - If subscriptions exist: List/ScrollView of subscription cards, each showing:
      - Community name (looked up from communityStore by communityID)
      - Current tier name and monthly price
      - "Subscribed since [date]" in caption style (format subscribedAt as "MMM d, yyyy")
      - Tappable row that navigates to that community's preview page (NavigationLink value: HubsRoute.communityPreview(id: communityID.uuidString))
    - Total monthly cost summary at bottom: "Total: $X.XX/mo" summing all subscription monthlyPrices.
    - #if DEBUG section at bottom: "Reset All Subscriptions" button (BlossomGhostButton, red text) that calls subscriptionStore.resetAll(). Add a confirmation alert before resetting.

    Modify HubsView.swift:
    - Add `.navigationDestination(for: HubsRoute.self)` case for `.mySubscriptions` that presents MySubscriptionsView.
    - Add a way to access My Subscriptions: place a small gear or person icon button in the Hubs top nav bar area. When tapped, pushes HubsRoute.mySubscriptions onto the navigation path. If there's already a top-bar pattern, follow it. Otherwise add a trailing nav button using `.toolbar { ToolbarItem(placement: .topBarTrailing) { ... } }`.

    Register MySubscriptionsView.swift in project.pbxproj under Features/Hubs/Subscription/ group.
  </action>
  <verify>
    <automated>xcodebuild build -scheme BlossomHubs -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5</automated>
  </verify>
  <done>My Subscriptions screen accessible from Hubs nav bar. Lists all active subscriptions with community name, tier, price, and subscription date. Total monthly cost displayed. Tapping a subscription navigates to community preview. Debug reset button clears all subscriptions with confirmation alert.</done>
</task>

</tasks>

<verification>
- Subscribed community: CTA reads "Your Subscription", tier sheet shows current plan
- Upgrade alert: "Upgrade to [Tier] for $X/mo?" with Confirm/Cancel
- Downgrade alert: "Downgrade to [Tier] for $X/mo?" with Confirm/Cancel
- Tier changes update immediately in SubscriptionStore and persist across restarts
- Cancel flow: retention sheet -> reason selection -> confirm -> subscription removed
- After cancel: CTA reverts to "View Tiers", badge removed from discovery card
- My Subscriptions shows all active subscriptions with totals
- Debug reset clears everything with confirmation
- Project builds with zero errors and zero Swift 6 warnings
</verification>

<success_criteria>
- User can upgrade tier, downgrade tier, or cancel from within the app (SUBS-08)
- Upgrade/downgrade uses alert confirmation dialogs
- Cancel uses retention bottom sheet with reason options
- My Subscriptions screen provides full subscription overview
- All subscription state changes persist via UserDefaults
- Debug reset toggle available for demo purposes
</success_criteria>

<output>
After completion, create `.planning/phases/04-subscription-flow-and-celebration/04-03-SUMMARY.md`
</output>
