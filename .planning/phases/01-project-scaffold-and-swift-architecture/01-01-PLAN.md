---
phase: 01-project-scaffold-and-swift-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BlossomHubs/BlossomHubs.xcodeproj
  - BlossomHubs/App/BlossomHubsApp.swift
  - BlossomHubs/Features/TabBar/TabItem.swift
  - BlossomHubs/Core/Theme/BlossomTheme.swift
  - BlossomHubs/Assets.xcassets/AppIcon.appiconset/Contents.json
  - BlossomHubs/Assets.xcassets/AccentColor.colorset/Contents.json
autonomous: true
requirements:
  - FOUND-01
  - FOUND-07
  - FOUND-08

must_haves:
  truths:
    - "App compiles and builds without errors targeting iOS 26 / Swift 6.2"
    - "ComponentsKit SPM dependency resolves and the project builds cleanly"
    - "Swift Strict Concurrency is set to Complete with zero warnings"
    - "App launches in Simulator and displays a root view"
  artifacts:
    - path: "BlossomHubs/BlossomHubs.xcodeproj"
      provides: "Xcode project with correct bundle ID and deployment target"
    - path: "BlossomHubs/App/BlossomHubsApp.swift"
      provides: "App entry point with UITabBar appearance suppression and light mode default"
      contains: "UITabBarAppearance"
    - path: "BlossomHubs/Features/TabBar/TabItem.swift"
      provides: "AppTab enum with all 6 tabs, icons, and labels"
      contains: "enum AppTab"
    - path: "BlossomHubs/Core/Theme/BlossomTheme.swift"
      provides: "Brand color constants for Teal, Violet, Orange, Dark Navy, Slate"
      contains: "35C7B2"
  key_links:
    - from: "BlossomHubs/App/BlossomHubsApp.swift"
      to: "UITabBar.appearance()"
      via: "init() block before first render"
      pattern: "UITabBarAppearance.*configureWithOpaqueBackground"
---

<objective>
Create the BlossomHubs Xcode project from scratch with correct iOS 26 deployment target, Swift 6.2 strict concurrency set to "Complete", ComponentsKit integrated via SPM, core type definitions (AppTab enum, BlossomTheme color constants), and the app entry point with Liquid Glass suppression and light-mode default.

Purpose: Establishes the build-able project shell that Plan 02 populates with the tab bar UI and navigation architecture. Without this, nothing compiles.
Output: An Xcode project that opens, builds, and launches in Simulator showing a minimal root view.
</objective>

<execution_context>
@/Users/nsimpson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nsimpson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffold-and-swift-architecture/01-CONTEXT.md
@.planning/phases/01-project-scaffold-and-swift-architecture/01-RESEARCH.md
@.planning/phases/01-project-scaffold-and-swift-architecture/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Xcode project with build settings, SPM dependency, and app entry point</name>
  <files>
    BlossomHubs/BlossomHubs.xcodeproj,
    BlossomHubs/App/BlossomHubsApp.swift,
    BlossomHubs/Assets.xcassets/AppIcon.appiconset/Contents.json,
    BlossomHubs/Assets.xcassets/AccentColor.colorset/Contents.json
  </files>
  <action>
    Create a new Xcode SwiftUI app project named "BlossomHubs" in the repo root directory.

    **Project settings:**
    - Product name: BlossomHubs
    - Display name: Blossom Hubs
    - Bundle identifier: com.blossom.hubs-prototype
    - Deployment target: iOS 26.0
    - Swift Language Version: 6.2 (or latest Swift 6)
    - Interface: SwiftUI
    - Lifecycle: SwiftUI App

    **Build settings (on the app target):**
    - Swift Strict Concurrency Checking: Complete (SWIFT_STRICT_CONCURRENCY = complete)
    - This MUST be set before any code is written to catch issues immediately

    **SPM dependency:**
    - Add ComponentsKit via File > Add Package Dependencies
    - URL: https://github.com/componentskit/ComponentsKit.git
    - Version rule: Up to Next Major from 1.6.1
    - Add `import ComponentsKit` in BlossomHubsApp.swift to verify it resolves (can remove later)

    **App entry point (BlossomHubsApp.swift):**
    ```swift
    import SwiftUI
    import ComponentsKit

    @main
    struct BlossomHubsApp: App {
        init() {
            // Suppress iOS 26 Liquid Glass on native tab bar (belt-and-suspenders with custom tab bar)
            let appearance = UITabBarAppearance()
            appearance.configureWithOpaqueBackground()
            appearance.backgroundColor = UIColor.systemBackground
            UITabBar.appearance().standardAppearance = appearance
            UITabBar.appearance().scrollEdgeAppearance = appearance
        }

        var body: some Scene {
            WindowGroup {
                ContentView()
                    .preferredColorScheme(.light) // Default to light mode for demo
            }
        }
    }
    ```

    Create a minimal ContentView.swift that just shows Text("Blossom Hubs") so the app builds and launches.

    **App icon:**
    - Copy `brand-guidlines/logos/Blossom-logo-icon-square.png` into the AppIcon asset set
    - Configure Contents.json for the single-size 1024x1024 app icon format

    **AccentColor:**
    - Set to Teal #35C7B2 (sRGB: 53/255, 199/255, 178/255) for both light and dark appearances
  </action>
  <verify>
    <automated>cd /Users/nsimpson/Documents/GitHub/Blossom-Development && xcodebuild build -project BlossomHubs/BlossomHubs.xcodeproj -scheme BlossomHubs -destination 'platform=iOS Simulator,name=iPhone 16 Pro' 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Xcode project exists at BlossomHubs/BlossomHubs.xcodeproj
    - Project builds with zero errors and zero strict concurrency warnings
    - ComponentsKit SPM dependency resolves successfully
    - App launches in Simulator showing "Blossom Hubs" text
    - Bundle identifier is com.blossom.hubs-prototype
    - Deployment target is iOS 26
    - Strict concurrency is set to Complete
  </done>
</task>

<task type="auto">
  <name>Task 2: Create AppTab enum and BlossomTheme color constants</name>
  <files>
    BlossomHubs/Features/TabBar/TabItem.swift,
    BlossomHubs/Core/Theme/BlossomTheme.swift
  </files>
  <action>
    **AppTab enum (TabItem.swift):**
    Create the tab definition enum that the tab bar and ContentView will use in Plan 02.

    ```swift
    // Features/TabBar/TabItem.swift
    import SwiftUI

    enum AppTab: String, CaseIterable, Identifiable {
        case home = "Home"
        case hubs = "Hubs"
        case markets = "Markets"
        case learn = "Learn"
        case portfolio = "Portfolio"
        case insights = "Insights"

        var id: String { rawValue }

        var icon: String {
            switch self {
            case .home:      "house.fill"
            case .hubs:      "person.3.fill"
            case .markets:   "globe"
            case .learn:     "book.fill"
            case .portfolio: "arrow.triangle.2.circlepath"
            case .insights:  "bolt.fill"
            }
        }
    }
    ```

    Tab order in `allCases` must be: Home, Hubs, Markets, Learn, Portfolio, Insights (Hubs at position 2, not last). This is the order defined in the enum declaration and CaseIterable will iterate in declaration order.

    **BlossomTheme (BlossomTheme.swift):**
    Create a theme file with brand color constants as static Color properties. Use a Color extension with hex initializer.

    ```swift
    // Core/Theme/BlossomTheme.swift
    import SwiftUI

    enum BlossomTheme {
        static let teal = Color(hex: "#35C7B2")
        static let violet = Color(hex: "#7361F7")
        static let orange = Color(hex: "#FF7833")
        static let darkNavy = Color(hex: "#1E222A")
        static let slate = Color(hex: "#565E76")

        // Tab bar specific
        static let tabActive = teal
        static let tabInactive = Color(UIColor.systemGray3)
        static let tabBarBackground = Color(UIColor.systemBackground)
    }

    extension Color {
        init(hex: String) {
            let hex = hex.trimmingCharacters(in: CharacterSet(charactersIn: "#"))
            let scanner = Scanner(string: hex)
            var rgbValue: UInt64 = 0
            scanner.scanHexInt64(&rgbValue)
            let r = Double((rgbValue & 0xFF0000) >> 16) / 255.0
            let g = Double((rgbValue & 0x00FF00) >> 8) / 255.0
            let b = Double(rgbValue & 0x0000FF) / 255.0
            self.init(red: r, green: g, blue: b)
        }
    }
    ```

    These files establish the contracts that Plan 02 will build against. Ensure both files are added to the Xcode project target.
  </action>
  <verify>
    <automated>cd /Users/nsimpson/Documents/GitHub/Blossom-Development && xcodebuild build -project BlossomHubs/BlossomHubs.xcodeproj -scheme BlossomHubs -destination 'platform=iOS Simulator,name=iPhone 16 Pro' 2>&1 | tail -5</automated>
  </verify>
  <done>
    - AppTab enum exists with 6 cases in correct order (Home, Hubs, Markets, Learn, Portfolio, Insights)
    - Each tab has the correct SF Symbol icon name
    - BlossomTheme provides Teal, Violet, Orange, Dark Navy, Slate as static Color properties
    - Color hex initializer extension works
    - Project still builds with zero errors and zero warnings
  </done>
</task>

</tasks>

<verification>
- `xcodebuild build -project BlossomHubs/BlossomHubs.xcodeproj -scheme BlossomHubs -destination 'platform=iOS Simulator,name=iPhone 16 Pro'` completes with BUILD SUCCEEDED
- Zero warnings in build output (especially zero strict concurrency warnings)
- ComponentsKit appears in resolved SPM packages
- AppTab.allCases iterates in correct order
</verification>

<success_criteria>
- Xcode project builds cleanly targeting iOS 26 with Swift 6.2 strict concurrency Complete
- ComponentsKit SPM dependency resolves without errors
- AppTab enum and BlossomTheme are compiled into the target
- App launches in Simulator (even if just showing placeholder text)
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffold-and-swift-architecture/01-01-SUMMARY.md`
</output>
