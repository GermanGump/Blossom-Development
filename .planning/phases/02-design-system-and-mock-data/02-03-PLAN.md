---
phase: 02-design-system-and-mock-data
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - BlossomHubs/Models/Community.swift
  - BlossomHubs/Models/CommunityStore.swift
  - BlossomHubs/Assets.xcassets/bd-profile-pic.imageset/Contents.json
  - BlossomHubs/Assets.xcassets/brandon-profile-pic.imageset/Contents.json
  - BlossomHubs/Assets.xcassets/max-profile-pic.imageset/Contents.json
  - BlossomHubs/Assets.xcassets/moe-profile-pic.imageset/Contents.json
  - BlossomHubs/Assets.xcassets/canada-tshirt-profile-pic.imageset/Contents.json
  - BlossomHubs/Assets.xcassets/blossom-logo-light.imageset/Contents.json
  - BlossomHubs/Assets.xcassets/blossom-logo-dark.imageset/Contents.json
  - BlossomHubs/Assets.xcassets/blossom-logo-icon.imageset/Contents.json
  - BlossomHubs/App/BlossomHubsApp.swift
  - BlossomHubs/App/ContentView.swift
  - BlossomHubs/BlossomHubs.xcodeproj/project.pbxproj
autonomous: false
requirements: [FOUND-09, FOUND-10, FOUND-11]

must_haves:
  truths:
    - "CommunityStore provides at least 6 communities with creators, tiers, posts, threads, and FAQ entries"
    - "All 6 ambassador profile photos load from the asset catalog by name — no placeholder images"
    - "All 3 Blossom logo variants load from the asset catalog by name — no placeholder images"
    - "CommunityStore is injected via .environment() at app level and readable by any view"
  artifacts:
    - path: "BlossomHubs/Models/Community.swift"
      provides: "All data model structs (Community, Creator, Tier, Post, ForumThread, FAQEntry, ContentCollection, PostType)"
      exports: ["Community", "Creator", "Tier", "Post", "ForumThread", "FAQEntry"]
    - path: "BlossomHubs/Models/CommunityStore.swift"
      provides: "@MainActor @Observable class with 6 mock communities"
      exports: ["CommunityStore"]
      contains: "@MainActor"
    - path: "BlossomHubs/Assets.xcassets/bd-profile-pic.imageset/Contents.json"
      provides: "BD ambassador profile photo"
      contains: "bd-profile-pic"
    - path: "BlossomHubs/Assets.xcassets/blossom-logo-light.imageset/Contents.json"
      provides: "Blossom logo light mode variant"
      contains: "blossom-logo-light"
  key_links:
    - from: "BlossomHubs/App/BlossomHubsApp.swift"
      to: "CommunityStore"
      via: "@State private var store = CommunityStore()"
      pattern: "@State.*CommunityStore"
    - from: "BlossomHubs/App/ContentView.swift"
      to: "CommunityStore"
      via: ".environment(store) injection"
      pattern: "\\.environment\\(store\\)"
    - from: "BlossomHubs/Models/CommunityStore.swift"
      to: "Community.swift model structs"
      via: "makeMockData() returns [Community]"
      pattern: "Community\\("
---

<objective>
Create all data model types, seed 6 ambassador communities with realistic investing content, register all ambassador profile photos and logo assets in the asset catalog, and wire CommunityStore into the app via environment injection.

Purpose: Every feature phase (3-9) reads community data from CommunityStore via @Environment — this plan creates the single data layer that all features depend on. Ambassador photos and logos must be real images from the project's profiles-demos/ and brand-guidlines/logos/ directories.

Output: Community.swift (data models), CommunityStore.swift (observable store with 6 communities), 5 new profile photo imagesets, 3 logo imagesets, updated BlossomHubsApp.swift and ContentView.swift with environment injection.
</objective>

<execution_context>
@/Users/nsimpson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nsimpson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-and-mock-data/02-RESEARCH.md
@.planning/phases/02-design-system-and-mock-data/02-CONTEXT.md
@.planning/phases/02-design-system-and-mock-data/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (dependency): BlossomTheme tokens available -->
<!-- nick-profile-pic.imageset already exists from Phase 1 -->

From BlossomHubs/Assets.xcassets/nick-profile-pic.imageset/Contents.json (REFERENCE for imageset format):
```json
{
  "images": [
    { "filename": "nick-profile-pic.png", "idiom": "universal", "scale": "1x" },
    { "idiom": "universal", "scale": "2x" },
    { "idiom": "universal", "scale": "3x" }
  ],
  "info": { "author": "xcode", "version": 1 }
}
```

From BlossomHubs/App/BlossomHubsApp.swift (WILL BE MODIFIED):
```swift
@main
struct BlossomHubsApp: App {
    init() { /* UITabBarAppearance setup */ }
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
```

Source files for assets:
- profiles-demos/BD-profile-pic.png
- profiles-demos/brandon-profile-pic.png
- profiles-demos/max-profile-pic.png
- profiles-demos/Moe-profile-pic.png
- profiles-demos/Canadia in a T-shirt-proflie-pic.png
- profiles-demos/nick-profile-pic.png (already in asset catalog)
- brand-guidlines/logos/Blossom-logo-lightmode.png
- brand-guidlines/logos/Blossom-logo-darkmode.png
- brand-guidlines/logos/Blossom-logo-icon-square.png
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data models, CommunityStore, asset images, and app wiring</name>
  <files>
    BlossomHubs/Models/Community.swift,
    BlossomHubs/Models/CommunityStore.swift,
    BlossomHubs/Assets.xcassets/bd-profile-pic.imageset/Contents.json,
    BlossomHubs/Assets.xcassets/brandon-profile-pic.imageset/Contents.json,
    BlossomHubs/Assets.xcassets/max-profile-pic.imageset/Contents.json,
    BlossomHubs/Assets.xcassets/moe-profile-pic.imageset/Contents.json,
    BlossomHubs/Assets.xcassets/canada-tshirt-profile-pic.imageset/Contents.json,
    BlossomHubs/Assets.xcassets/blossom-logo-light.imageset/Contents.json,
    BlossomHubs/Assets.xcassets/blossom-logo-dark.imageset/Contents.json,
    BlossomHubs/Assets.xcassets/blossom-logo-icon.imageset/Contents.json,
    BlossomHubs/App/BlossomHubsApp.swift,
    BlossomHubs/App/ContentView.swift,
    BlossomHubs/BlossomHubs.xcodeproj/project.pbxproj
  </files>
  <action>
    **Step 1: Create Models/ directory** at BlossomHubs/Models/.

    **Step 2: Create Community.swift** with all data model structs. All structs are Identifiable with UUID ids. Use the exact structure from RESEARCH.md code examples:

    ```
    Creator: id, name, username, profileImageName (asset catalog name), isVerified, isAmbassador, bio
    Tier: id, name, monthlyPrice (Decimal), benefits ([String])
    Post: id, authorId, content, postType (enum: .text, .tradeHighlight, .youtubeLink), stockTickers ([String]), youtubeURL (String?), requiredTierIndex (Int), publishedAt (Date), collection (String?)
    ForumThread: id, title, content, authorId, requiredTierIndex, replyCount, likeCount, publishedAt
    FAQEntry: id, question, answer (String?), isAnswered, askedBy (String), answeredBy (String?)
    Community: id, name, description, logoImageName (String), bannerImageName (String?), creator, tiers, posts, threads, faqEntries, memberCount, category
    ```

    Add PostType enum: `.text`, `.tradeHighlight`, `.youtubeLink` — this enables content feed display logic in Phase 6.

    **Step 3: Create CommunityStore.swift** as @MainActor @Observable class:
    ```swift
    @MainActor
    @Observable
    final class CommunityStore {
        var communities: [Community] = CommunityStore.makeMockData()

        static func makeMockData() -> [Community] { ... }
    }
    ```

    Populate 6 communities — one per ambassador. Use RESEARCH.md mock data design:

    | Ambassador | Profile Image Name | Community Name | Tiers | Focus |
    |-----------|-------------------|----------------|-------|-------|
    | Nick | nick-profile-pic | Wealthmatica | 3 (Observer $0, Investor $19, Pro $49) | Canadian stocks, dividend investing |
    | BD | bd-profile-pic | BD Investing | 2 (Basic $9, Premium $39) | Growth stocks, tech analysis |
    | Brandon | brandon-profile-pic | Brandon's Alpha | 3 (Free $0, Core $25, Elite $75) | Options, swing trades |
    | Max | max-profile-pic | Max Markets | 2 (Member $9, Analyst $29) | Index funds, passive investing |
    | Moe | moe-profile-pic | Moe's Watchlist | 4 (Observer $0, Member $15, Pro $45, VIP $100) | Momentum trading, small-cap |
    | Canadian in a T-shirt | canada-tshirt-profile-pic | Canadian Investor | 3 (Basic $5, Investor $20, Mentor Circle $60) | TFSA/RRSP, TSX stocks |

    Each community must have:
    - 4-6 posts with realistic investing content, real stock tickers ($AAPL, $TSLA, $NVDA, $MSFT, $AMD, $SHOP.TO, $RY.TO), varied postTypes, varied requiredTierIndex (some free preview, some gated)
    - 3-5 forum threads with titles like "Best dividend ETFs for 2026?", "NVDA earnings play", "Portfolio review - feedback welcome"
    - 3-4 FAQ entries (some answered, some pending)
    - Tiers with realistic benefit descriptions (e.g., "Weekly market commentary", "Real-time trade alerts", "Monthly portfolio review call")
    - memberCount ranging 50-500

    All creators are isVerified: true and isAmbassador: true.

    **Step 4: Create 5 new profile photo imagesets** (nick already exists):
    - BlossomHubs/Assets.xcassets/bd-profile-pic.imageset/ — copy profiles-demos/BD-profile-pic.png into it as 1x
    - BlossomHubs/Assets.xcassets/brandon-profile-pic.imageset/ — copy profiles-demos/brandon-profile-pic.png
    - BlossomHubs/Assets.xcassets/max-profile-pic.imageset/ — copy profiles-demos/max-profile-pic.png
    - BlossomHubs/Assets.xcassets/moe-profile-pic.imageset/ — copy profiles-demos/Moe-profile-pic.png (note capital M in source)
    - BlossomHubs/Assets.xcassets/canada-tshirt-profile-pic.imageset/ — copy "profiles-demos/Canadia in a T-shirt-proflie-pic.png" (note typos in source filename are intentional)

    Each imageset gets a Contents.json matching nick-profile-pic.imageset format: 1x slot has filename, 2x and 3x slots empty.

    The actual .png file must be physically copied into the imageset directory (not just referenced). Use `cp` command.

    **Step 5: Create 3 logo imagesets:**
    - BlossomHubs/Assets.xcassets/blossom-logo-light.imageset/ — copy brand-guidlines/logos/Blossom-logo-lightmode.png
    - BlossomHubs/Assets.xcassets/blossom-logo-dark.imageset/ — copy brand-guidlines/logos/Blossom-logo-darkmode.png
    - BlossomHubs/Assets.xcassets/blossom-logo-icon.imageset/ — copy brand-guidlines/logos/Blossom-logo-icon-square.png

    Same Contents.json format as profile photos.

    **Step 6: Wire CommunityStore into app.**

    Update BlossomHubsApp.swift:
    - Add `@State private var store = CommunityStore()` property
    - Add `.environment(store)` modifier to ContentView() in body
    - Add `#if DEBUG` assertion: `assert(store.communities.count >= 3, "Mock data should have at least 3 communities")`
    - Note: Plan 01 may have already added font assertions — preserve those, add data assertion alongside

    Update ContentView.swift:
    - NO structural changes needed — .environment() passes through to all child views automatically
    - Optionally add `@Environment(CommunityStore.self) private var store` just to verify it compiles (can be removed after verification)

    **Step 7: Register all new files in project.pbxproj** using xcodeproj Ruby gem:
    - Create Models/ group under BlossomHubs
    - Add Community.swift and CommunityStore.swift to Models group and target source build phase
    - The imageset directories inside Assets.xcassets are automatically included since Assets.xcassets is already registered
    - But the actual .png files must exist inside the imageset directories for Xcode to find them
  </action>
  <verify>
    <automated>cd /Users/nsimpson/Documents/GitHub/Blossom-Development && echo "Models:" && ls BlossomHubs/Models/*.swift 2>/dev/null | wc -l && echo "Profile imagesets:" && ls -d BlossomHubs/Assets.xcassets/*-profile-pic.imageset 2>/dev/null | wc -l && echo "Logo imagesets:" && ls -d BlossomHubs/Assets.xcassets/blossom-logo-*.imageset 2>/dev/null | wc -l && echo "Profile photos copied:" && find BlossomHubs/Assets.xcassets -name "*-profile-pic.png" | wc -l && echo "Store class:" && grep -c "@MainActor" BlossomHubs/Models/CommunityStore.swift 2>/dev/null && echo "Environment:" && grep -c "environment(store)" BlossomHubs/App/BlossomHubsApp.swift 2>/dev/null</automated>
  </verify>
  <done>2 model files exist in Models/. 6 profile photo imagesets exist (5 new + nick). 3 logo imagesets exist. All .png files physically copied into imageset directories. CommunityStore is @MainActor @Observable with 6 communities. BlossomHubsApp injects store via .environment(). Debug assertion verifies community count.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of design system and mock data</name>
  <what-built>Complete design system (colors, fonts, components) and mock data layer with ambassador photos and logos. All Phase 1 views retrofitted to use Inter font and brand color tokens. Dark mode enabled (preferredColorScheme(.light) removed).</what-built>
  <how-to-verify>
    1. Open BlossomHubs/BlossomHubs.xcodeproj in Xcode
    2. Resolve SPM packages if prompted, then Build (Cmd+B) — should succeed with zero errors
    3. Run on iPhone simulator (Cmd+R)
    4. Verify the Hubs tab shows: Nick's profile photo (real photo, not SF Symbol placeholder), Inter font on all text (distinct from SF Pro — rounder letterforms, look at the "e" character)
    5. Open Settings app in simulator > Display & Brightness > toggle Dark Mode ON
    6. Return to Blossom Hubs — verify: dark background (#1E222A), no white-on-white text, no invisible elements, tab bar adapts
    7. In Xcode console output, confirm "Inter" appears in font family names (debug print)
    8. Verify no assertion failures in console (font loading + community count assertions)
  </how-to-verify>
  <resume-signal>Type "approved" to continue to Phase 3, or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
- Community.swift compiles with all 6 struct types (Community, Creator, Tier, Post, ForumThread, FAQEntry)
- CommunityStore has exactly 6 communities, each with tiers, posts, threads, FAQ entries
- All 6 ambassador photos exist as imagesets with .png files physically present
- All 3 logo variants exist as imagesets with .png files physically present
- BlossomHubsApp.swift injects CommunityStore via .environment()
- No assertion failures at app launch (fonts loaded, community count >= 3)
</verification>

<success_criteria>
- Mock data layer provides 6 communities with rich, realistic investing content
- All ambassador profile photos render from asset catalog (Image("bd-profile-pic") etc.)
- All Blossom logo variants render from asset catalog
- CommunityStore accessible from any view via @Environment(CommunityStore.self)
- Human visual verification confirms: Inter font, dark mode, photos, no build errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system-and-mock-data/02-03-SUMMARY.md`
</output>
