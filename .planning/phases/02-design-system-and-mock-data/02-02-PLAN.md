---
phase: 02-design-system-and-mock-data
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - BlossomHubs/Core/Components/BlossomCard.swift
  - BlossomHubs/Core/Components/BlossomButton.swift
  - BlossomHubs/Core/Components/VerifiedBadge.swift
  - BlossomHubs/Core/Components/TagView.swift
  - BlossomHubs/Core/Components/SectionHeader.swift
  - BlossomHubs/Core/Components/EmptyStateView.swift
  - BlossomHubs/Core/Components/LockedContentOverlay.swift
  - BlossomHubs/Core/Components/AvatarView.swift
  - BlossomHubs/Core/Components/PlaceholderTabView.swift
  - BlossomHubs/Features/TabBar/BlossomTabBar.swift
  - BlossomHubs/Features/Hubs/HubsView.swift
  - BlossomHubs/Features/Hubs/HubsTopNavBar.swift
  - BlossomHubs/BlossomHubs.xcodeproj/project.pbxproj
autonomous: true
requirements: [FOUND-04]

must_haves:
  truths:
    - "Every shared UI primitive (card, button, badge, tag, section header, empty state, locked overlay) exists as a reusable component"
    - "All existing Phase 1 views use BlossomFont instead of system fonts"
    - "AvatarView supports size presets and a verified badge option"
  artifacts:
    - path: "BlossomHubs/Core/Components/BlossomCard.swift"
      provides: "Card ViewModifier with background, border, corner radius, shadow"
      exports: ["BlossomCard", "blossomCard"]
    - path: "BlossomHubs/Core/Components/BlossomButton.swift"
      provides: "Primary, secondary, ghost ButtonStyle implementations"
      exports: ["BlossomPrimaryButton", "BlossomSecondaryButton", "BlossomGhostButton"]
    - path: "BlossomHubs/Core/Components/VerifiedBadge.swift"
      provides: "Teal shield checkmark badge matching real Blossom app"
      exports: ["VerifiedBadge"]
    - path: "BlossomHubs/Core/Components/TagView.swift"
      provides: "Pill-shaped labels with .stock, .tier, .category styles"
      exports: ["TagView", "TagStyle"]
    - path: "BlossomHubs/Core/Components/SectionHeader.swift"
      provides: "Title + optional trailing action button"
      exports: ["SectionHeader"]
    - path: "BlossomHubs/Core/Components/EmptyStateView.swift"
      provides: "SF Symbol + title + subtitle + optional CTA"
      exports: ["EmptyStateView"]
    - path: "BlossomHubs/Core/Components/LockedContentOverlay.swift"
      provides: "Blur + lock icon + tier upgrade prompt"
      exports: ["LockedContentOverlay"]
  key_links:
    - from: "BlossomHubs/Core/Components/BlossomCard.swift"
      to: "BlossomTheme.cardSurface, BlossomTheme.cardBorder"
      via: "Color token references"
      pattern: "BlossomTheme\\.(cardSurface|cardBorder)"
    - from: "BlossomHubs/Core/Components/BlossomButton.swift"
      to: "BlossomTheme.violet, BlossomFont.buttonLabel"
      via: "Theme and font token references"
      pattern: "BlossomTheme\\.violet"
    - from: "BlossomHubs/Features/Hubs/HubsView.swift"
      to: "BlossomFont"
      via: "Retrofit from system fonts"
      pattern: "BlossomFont\\."
---

<objective>
Build all shared UI component primitives and retrofit existing Phase 1 views to use the new design tokens.

Purpose: Feature phases (3-9) need these building blocks to compose screens without inline styling. Retrofitting Phase 1 views ensures visual consistency — no mixing of SF Pro and Inter, no raw hex colors.

Output: 7 new component files (BlossomCard, BlossomButton, VerifiedBadge, TagView, SectionHeader, EmptyStateView, LockedContentOverlay), enhanced AvatarView, retrofitted PlaceholderTabView/BlossomTabBar/HubsView/HubsTopNavBar.
</objective>

<execution_context>
@/Users/nsimpson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nsimpson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-and-mock-data/02-RESEARCH.md
@.planning/phases/02-design-system-and-mock-data/02-CONTEXT.md
@.planning/phases/02-design-system-and-mock-data/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (dependency): BlossomTheme tokens available after refactor -->

From BlossomHubs/Core/Theme/BlossomTheme.swift (after Plan 01 refactor):
```swift
enum BlossomTheme {
    static let teal = Color("BlossomTeal")
    static let violet = Color("BlossomViolet")
    static let orange = Color("BlossomOrange")
    static let darkNavy = Color("BlossomDarkNavy")
    static let slate = Color("BlossomSlate")
    static let background = Color("BlossomBackground")
    static let cardSurface = Color("BlossomCardSurface")
    static let cardBorder = Color("BlossomCardBorder")
    static let primaryText = Color("BlossomPrimaryText")
    static let secondaryText = Color("BlossomSecondaryText")
    static let tabActive = teal
    static let tabInactive = Color(UIColor.systemGray3)
    static let tabBarBackground = Color(UIColor.systemBackground)
}
```

From BlossomHubs/Core/Theme/BlossomFont.swift (after Plan 01):
```swift
enum BlossomFont {
    static var largeTitle: Font { Font.custom("Inter-SemiBold", size: 34) }
    static var title: Font { Font.custom("Inter-SemiBold", size: 28) }
    static var headline: Font { Font.custom("Inter-SemiBold", size: 17) }
    static var subhead: Font { Font.custom("Inter-Medium", size: 15) }
    static var body: Font { Font.custom("Inter-Regular", size: 17) }
    static var callout: Font { Font.custom("Inter-Regular", size: 16) }
    static var caption: Font { Font.custom("Inter-Regular", size: 12) }
    static var buttonLabel: Font { Font.custom("Inter-Medium", size: 16) }
}
```

From BlossomHubs/Core/Components/AvatarView.swift (WILL BE ENHANCED):
```swift
struct AvatarView: View {
    let image: Image
    var ringColor: Color = BlossomTheme.teal
    var showBadge: Bool = false
    var size: CGFloat = 40
}
```

From BlossomHubs/Core/Components/PlaceholderTabView.swift (WILL BE RETROFITTED):
Uses system fonts — must switch to BlossomFont.

From BlossomHubs/Features/TabBar/BlossomTabBar.swift (WILL BE RETROFITTED):
Uses system fonts — must switch to BlossomFont.

From BlossomHubs/Features/Hubs/HubsView.swift (WILL BE RETROFITTED):
Uses system fonts — must switch to BlossomFont.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 7 shared UI component primitives</name>
  <files>
    BlossomHubs/Core/Components/BlossomCard.swift,
    BlossomHubs/Core/Components/BlossomButton.swift,
    BlossomHubs/Core/Components/VerifiedBadge.swift,
    BlossomHubs/Core/Components/TagView.swift,
    BlossomHubs/Core/Components/SectionHeader.swift,
    BlossomHubs/Core/Components/EmptyStateView.swift,
    BlossomHubs/Core/Components/LockedContentOverlay.swift,
    BlossomHubs/BlossomHubs.xcodeproj/project.pbxproj
  </files>
  <action>
    Create 7 new Swift files in BlossomHubs/Core/Components/. All must use BlossomTheme tokens and BlossomFont — zero inline hex, zero system fonts.

    **1. BlossomCard.swift** — ViewModifier:
    - Background: BlossomTheme.cardSurface
    - Border: 1px BlossomTheme.cardBorder, 12px corner radius (RoundedRectangle overlay)
    - Shadow: .black.opacity(0.06), radius 4, x:0, y:2
    - Extension: `func blossomCard() -> some View { modifier(BlossomCard()) }`
    - Include #Preview showing a Text in a card

    **2. BlossomButton.swift** — 3 ButtonStyle conformances:
    - BlossomPrimaryButton: Violet fill (#7361F7 via BlossomTheme.violet), white label, Inter-Medium 16pt (BlossomFont.buttonLabel), 8px corner radius, full-width with 16px vertical padding. Pressed state: 0.9 opacity.
    - BlossomSecondaryButton: Clear background, Violet border (1.5px), Violet text, same corner radius and padding. Pressed state: 0.9 opacity.
    - BlossomGhostButton: No background, teal text (BlossomTheme.teal), no border. Pressed state: 0.7 opacity.
    - Study brand-guidelines/app-screenshots/ for exact button styles if available, but these specs match the real Blossom app.

    **3. VerifiedBadge.swift** — View:
    - Teal capsule/pill shape with light teal background fill (BlossomTheme.teal.opacity(0.15))
    - HStack: SF Symbol "checkmark.shield.fill" in teal + "Verified" text in teal, caption size (BlossomFont.caption)
    - Horizontal padding 8, vertical padding 4
    - Match the visual from brand-guidlines/app-screenshots/close-up-profile-sample.png

    **4. TagView.swift** — View with TagStyle enum:
    - TagStyle enum: .stock (orange background tint, orange text), .tier (violet background tint, violet text), .category (slate background tint, primary text)
    - Pill shape: text with horizontal padding 10, vertical padding 4, capsule background at 0.12 opacity of the style color
    - Font: BlossomFont.caption
    - Init takes (text: String, style: TagStyle)

    **5. SectionHeader.swift** — View:
    - HStack: title text (BlossomFont.headline, BlossomTheme.primaryText) + Spacer + optional trailing action button (text in teal, BlossomFont.subhead)
    - Init: (title: String, actionText: String? = nil, action: (() -> Void)? = nil)
    - Horizontal padding 16

    **6. EmptyStateView.swift** — View:
    - VStack(spacing: 16): SF Symbol icon (44pt, secondaryText color) + title (BlossomFont.headline, primaryText) + subtitle (BlossomFont.body, secondaryText) + optional CTA button (BlossomPrimaryButton style)
    - Centered in available space with padding
    - Init: (icon: String, title: String, subtitle: String, ctaText: String? = nil, ctaAction: (() -> Void)? = nil)

    **7. LockedContentOverlay.swift** — View:
    - ZStack: blurred content behind (use .blur(radius: 8) + .opacity(0.6) on content) + overlay VStack with lock icon (SF Symbol "lock.fill", 32pt), "Upgrade to {tierName}" headline, "{tierName} members get access" body text, "Upgrade" CTA button (BlossomPrimaryButton)
    - Dark scrim behind overlay text for readability: Color.black.opacity(0.3)
    - Init: (tierName: String, onUpgrade: @escaping () -> Void, @ViewBuilder content: () -> Content) where Content: View

    **Register all 7 files** in project.pbxproj using xcodeproj Ruby gem — add to Core/Components group and target source build phase.
  </action>
  <verify>
    <automated>cd /Users/nsimpson/Documents/GitHub/Blossom-Development && ls BlossomHubs/Core/Components/Blossom*.swift BlossomHubs/Core/Components/Verified*.swift BlossomHubs/Core/Components/Tag*.swift BlossomHubs/Core/Components/Section*.swift BlossomHubs/Core/Components/Empty*.swift BlossomHubs/Core/Components/Locked*.swift 2>/dev/null | wc -l</automated>
  </verify>
  <done>7 new component files exist in Core/Components/. Each uses BlossomTheme tokens and BlossomFont — zero inline hex values. All registered in project.pbxproj source build phase.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance AvatarView and retrofit Phase 1 views to design tokens</name>
  <files>
    BlossomHubs/Core/Components/AvatarView.swift,
    BlossomHubs/Core/Components/PlaceholderTabView.swift,
    BlossomHubs/Features/TabBar/BlossomTabBar.swift,
    BlossomHubs/Features/Hubs/HubsView.swift,
    BlossomHubs/Features/Hubs/HubsTopNavBar.swift
  </files>
  <action>
    **Step 1: Enhance AvatarView.swift.**
    - Add AvatarSize enum with presets: .small (28), .medium (40), .large (56), .xlarge (80) — each maps to a CGFloat
    - Add convenience init: `init(imageName: String, preset: AvatarSize, ...)` that uses Image(imageName) and preset.rawValue for size
    - KEEP the existing `size: CGFloat = 40` parameter for backward compatibility (HubsTopNavBar passes size: 44)
    - Add `showVerifiedBadge: Bool = false` parameter — when true, show a small VerifiedBadge positioned at bottom-trailing of the avatar
    - The existing bolt badge (showBadge) and new verified badge are separate — verified badge is for creator profiles, bolt badge is for ambassador highlight
    - Update #Preview to show both badge variants

    **Step 2: Retrofit PlaceholderTabView.swift.**
    - Replace all .font(.system(...)) or .font(.title) calls with BlossomFont equivalents
    - Replace any hardcoded Color values with BlossomTheme tokens
    - Use BlossomTheme.primaryText for titles, BlossomTheme.secondaryText for subtitles
    - Use BlossomTheme.background for the background

    **Step 3: Retrofit BlossomTabBar.swift.**
    - Replace any .font(.system(size:)) calls with BlossomFont.caption
    - Replace any hardcoded colors with BlossomTheme tokens (tabActive/tabInactive already use BlossomTheme, but check for any inline hex or system colors)
    - Use BlossomTheme.tabBarBackground for the bar background

    **Step 4: Retrofit HubsView.swift.**
    - Replace all system font modifiers with BlossomFont
    - Replace any inline Color values with BlossomTheme tokens
    - Use BlossomTheme.background for the view background

    **Step 5: Retrofit HubsTopNavBar.swift.**
    - Replace all system font modifiers with BlossomFont
    - Replace any inline Color values with BlossomTheme tokens (the search field, bell badge, etc.)
    - Verify AvatarView call still works (size: 44 as CGFloat should remain compatible)

    No new file registrations needed — these are all existing files being modified.
  </action>
  <verify>
    <automated>cd /Users/nsimpson/Documents/GitHub/Blossom-Development && grep -rn '\.font(\.system\|\.font(\.title\|\.font(\.body\|\.font(\.caption' BlossomHubs/Core/Components/PlaceholderTabView.swift BlossomHubs/Features/TabBar/BlossomTabBar.swift BlossomHubs/Features/Hubs/HubsView.swift BlossomHubs/Features/Hubs/HubsTopNavBar.swift 2>/dev/null | wc -l</automated>
  </verify>
  <done>Zero system font references remain in PlaceholderTabView, BlossomTabBar, HubsView, HubsTopNavBar — all use BlossomFont. AvatarView has AvatarSize presets and showVerifiedBadge parameter while maintaining backward compatibility with existing callers.</done>
</task>

</tasks>

<verification>
- 7 new component files compile (structural check via xcodeproj gem)
- Zero system font references (.font(.system), .font(.title), .font(.body)) in any retrofitted file
- Zero inline hex Color values in any component or retrofitted file
- AvatarView backward compatible — HubsTopNavBar still passes size: 44 as CGFloat
- Each component has at least one #Preview
</verification>

<success_criteria>
- All 7 shared UI primitives exist as reusable SwiftUI components using BlossomTheme and BlossomFont tokens
- AvatarView enhanced with size presets and verified badge while maintaining backward compatibility
- All Phase 1 views retrofitted to use BlossomFont — no SF Pro rendering in any view
- All components use BlossomTheme semantic tokens — dark mode will adapt automatically
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system-and-mock-data/02-02-SUMMARY.md`
</output>
