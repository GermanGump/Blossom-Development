---
phase: 02-design-system-and-mock-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BlossomHubs/Assets.xcassets/Colors/BlossomTeal.colorset/Contents.json
  - BlossomHubs/Assets.xcassets/Colors/BlossomViolet.colorset/Contents.json
  - BlossomHubs/Assets.xcassets/Colors/BlossomOrange.colorset/Contents.json
  - BlossomHubs/Assets.xcassets/Colors/BlossomDarkNavy.colorset/Contents.json
  - BlossomHubs/Assets.xcassets/Colors/BlossomSlate.colorset/Contents.json
  - BlossomHubs/Assets.xcassets/Colors/BlossomBackground.colorset/Contents.json
  - BlossomHubs/Assets.xcassets/Colors/BlossomCardSurface.colorset/Contents.json
  - BlossomHubs/Assets.xcassets/Colors/BlossomCardBorder.colorset/Contents.json
  - BlossomHubs/Assets.xcassets/Colors/BlossomPrimaryText.colorset/Contents.json
  - BlossomHubs/Assets.xcassets/Colors/BlossomSecondaryText.colorset/Contents.json
  - BlossomHubs/Inter-Regular.otf
  - BlossomHubs/Inter-Medium.otf
  - BlossomHubs/Inter-SemiBold.otf
  - BlossomHubs/Info.plist
  - BlossomHubs/Core/Theme/BlossomFont.swift
  - BlossomHubs/Core/Theme/BlossomTheme.swift
  - BlossomHubs/BlossomHubs.xcodeproj/project.pbxproj
autonomous: true
requirements: [FOUND-04, FOUND-05, FOUND-06]

must_haves:
  truths:
    - "All brand colors resolve from Asset Catalog named color sets — no inline hex Color values in BlossomTheme"
    - "Switching between light and dark mode causes background, card surface, card border, and text colors to adapt automatically"
    - "All text renders in Inter font (Regular, Medium, Semi-Bold) via BlossomFont enum — no system font fallback"
  artifacts:
    - path: "BlossomHubs/Assets.xcassets/Colors/BlossomTeal.colorset/Contents.json"
      provides: "Teal brand accent color — identical light and dark"
      contains: "0.208"
    - path: "BlossomHubs/Assets.xcassets/Colors/BlossomBackground.colorset/Contents.json"
      provides: "Page background — white in light, dark navy in dark"
      contains: "luminosity"
    - path: "BlossomHubs/Core/Theme/BlossomFont.swift"
      provides: "Centralized font enum with all type scale levels"
      exports: ["BlossomFont"]
      contains: "Inter-SemiBold"
    - path: "BlossomHubs/Core/Theme/BlossomTheme.swift"
      provides: "Refactored theme using Color() asset catalog lookups"
      contains: 'Color("BlossomTeal")'
    - path: "BlossomHubs/Info.plist"
      provides: "Inter font registration"
      contains: "UIAppFonts"
  key_links:
    - from: "BlossomHubs/Core/Theme/BlossomTheme.swift"
      to: "Assets.xcassets/Colors/*.colorset"
      via: 'Color("name") lookups'
      pattern: 'Color\("Blossom'
    - from: "BlossomHubs/Core/Theme/BlossomFont.swift"
      to: "Inter-*.otf bundle resources"
      via: "Font.custom() with PostScript names"
      pattern: 'Font\.custom\("Inter-'
---

<objective>
Create the color and typography foundation for the entire design system: 10 Asset Catalog color sets with light/dark variants, Inter font bundle registration, BlossomFont enum, and BlossomTheme refactor from inline hex to named color lookups.

Purpose: Every subsequent component and feature view depends on these tokens existing. Colors must be in the asset catalog BEFORE any code references them (Color("name") silently returns transparent if the colorset doesn't exist). Fonts must be registered BEFORE BlossomFont enum is created.

Output: 10 colorsets in Assets.xcassets, 3 Inter .otf files in bundle resources, BlossomFont.swift, refactored BlossomTheme.swift, updated Info.plist and project.pbxproj.
</objective>

<execution_context>
@/Users/nsimpson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nsimpson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-and-mock-data/02-RESEARCH.md
@.planning/phases/02-design-system-and-mock-data/02-CONTEXT.md
@.planning/phases/01-project-scaffold-and-swift-architecture/01-01-SUMMARY.md

<interfaces>
<!-- Existing code this plan modifies -->

From BlossomHubs/Core/Theme/BlossomTheme.swift (WILL BE REFACTORED):
```swift
enum BlossomTheme {
    static let teal = Color(hex: "#35C7B2")
    static let violet = Color(hex: "#7361F7")
    static let orange = Color(hex: "#FF7833")
    static let darkNavy = Color(hex: "#1E222A")
    static let slate = Color(hex: "#565E76")
    static let tabActive = teal
    static let tabInactive = Color(UIColor.systemGray3)
    static let tabBarBackground = Color(UIColor.systemBackground)
}
```

From BlossomHubs/Info.plist (WILL ADD UIAppFonts):
Currently has no UIAppFonts key — font registration must be added.

From BlossomHubs/Assets.xcassets/AccentColor.colorset/Contents.json (REFERENCE for colorset format):
Uses sRGB color-space with decimal components (0.0-1.0), "idiom": "universal", dark variant via "appearances": [{"appearance": "luminosity", "value": "dark"}].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Asset Catalog color sets and Inter font registration</name>
  <files>
    BlossomHubs/Assets.xcassets/Colors/BlossomTeal.colorset/Contents.json,
    BlossomHubs/Assets.xcassets/Colors/BlossomViolet.colorset/Contents.json,
    BlossomHubs/Assets.xcassets/Colors/BlossomOrange.colorset/Contents.json,
    BlossomHubs/Assets.xcassets/Colors/BlossomDarkNavy.colorset/Contents.json,
    BlossomHubs/Assets.xcassets/Colors/BlossomSlate.colorset/Contents.json,
    BlossomHubs/Assets.xcassets/Colors/BlossomBackground.colorset/Contents.json,
    BlossomHubs/Assets.xcassets/Colors/BlossomCardSurface.colorset/Contents.json,
    BlossomHubs/Assets.xcassets/Colors/BlossomCardBorder.colorset/Contents.json,
    BlossomHubs/Assets.xcassets/Colors/BlossomPrimaryText.colorset/Contents.json,
    BlossomHubs/Assets.xcassets/Colors/BlossomSecondaryText.colorset/Contents.json,
    BlossomHubs/Assets.xcassets/Colors/Contents.json,
    BlossomHubs/Inter-Regular.otf,
    BlossomHubs/Inter-Medium.otf,
    BlossomHubs/Inter-SemiBold.otf,
    BlossomHubs/Info.plist,
    BlossomHubs/BlossomHubs.xcodeproj/project.pbxproj
  </files>
  <action>
    **Step 1: Create 10 colorset directories** under BlossomHubs/Assets.xcassets/Colors/. Each colorset directory gets a Contents.json. Also create BlossomHubs/Assets.xcassets/Colors/Contents.json as a group folder marker ({"info":{"author":"xcode","version":1},"properties":{"provides-namespace":true}}).

    Color specifications (sRGB decimal components — hex/255):

    Brand accents (IDENTICAL light and dark per user decision):
    - BlossomTeal: R:0.208 G:0.780 B:0.698 (both modes)
    - BlossomViolet: R:0.451 G:0.380 B:0.969 (both modes)
    - BlossomOrange: R:1.000 G:0.471 B:0.200 (both modes)
    - BlossomDarkNavy: R:0.118 G:0.133 B:0.165 (both modes)
    - BlossomSlate: R:0.337 G:0.369 B:0.463 (both modes)

    Semantic tokens (DIFFERENT light vs dark):
    - BlossomBackground: Light R:1.000 G:1.000 B:1.000 / Dark R:0.118 G:0.133 B:0.165
    - BlossomCardSurface: Light R:1.000 G:1.000 B:1.000 / Dark R:0.165 G:0.180 B:0.220
    - BlossomCardBorder: Light R:0.886 G:0.894 B:0.914 / Dark R:0.227 G:0.243 B:0.282
    - BlossomPrimaryText: Light R:0.118 G:0.133 B:0.165 / Dark R:1.000 G:1.000 B:1.000
    - BlossomSecondaryText: Light R:0.337 G:0.369 B:0.463 / Dark R:0.545 G:0.573 B:0.659

    Format: replicate AccentColor.colorset/Contents.json exactly. Each file has "colors" array with universal entry (light) and appearances[luminosity:dark] entry (dark). For brand accents where both modes are identical, still include both entries with the same values.

    NOTE: The Colors/ group folder needs provides-namespace:true so colors are accessed as Color("BlossomTeal") not Color("Colors/BlossomTeal") — actually, Asset Catalog namespace scoping only applies to images. Color sets are always accessed by their .colorset folder name regardless of group nesting. So the provides-namespace property is NOT needed for the Colors group — just use the standard {"info":{"author":"xcode","version":1}} for the group Contents.json.

    **Step 2: Copy Inter font files.** The Inter font .otf files need to be obtained. Check if they exist in the project already. If not, download Inter from Google Fonts (https://fonts.google.com/specimen/Inter) or use the files from a local font cache. Place Inter-Regular.otf, Inter-Medium.otf, Inter-SemiBold.otf directly in BlossomHubs/ (adjacent to Assets.xcassets).

    If Inter .otf files cannot be found locally, create a placeholder note and mark the task as needing user action to provide the font files. The files MUST have the exact PostScript names "Inter-Regular", "Inter-Medium", "Inter-SemiBold" for Font.custom() to work.

    **Step 3: Update Info.plist.** Add UIAppFonts key with array of 3 strings: "Inter-Regular.otf", "Inter-Medium.otf", "Inter-SemiBold.otf".

    **Step 4: Register everything in project.pbxproj** using the xcodeproj Ruby gem:
    - Create a "Colors" group under Assets.xcassets (the colorset directories)
    - Add 3 .otf font files as file references in the BlossomHubs group
    - Add font file references to target.resources_build_phase (Copy Bundle Resources)
    - The colorset directories inside Assets.xcassets are automatically included since Assets.xcassets is already registered as a resources folder

    Use the same xcodeproj Ruby gem pattern from Phase 1 (create_project.rb). Create a new Ruby script (e.g., phase2_register.rb) that opens the existing project and adds the new references.
  </action>
  <verify>
    <automated>cd /Users/nsimpson/Documents/GitHub/Blossom-Development && ruby -e "require 'xcodeproj'; p = Xcodeproj::Project.open('BlossomHubs/BlossomHubs.xcodeproj'); refs = p.targets.first.resources_build_phase.files.map{|f| f.file_ref.path rescue nil}.compact; puts 'Font refs: ' + refs.select{|r| r.include?('Inter')}.inspect; puts 'Colors exist: ' + Dir.exist?('BlossomHubs/Assets.xcassets/Colors').to_s; puts 'Colorset count: ' + Dir.glob('BlossomHubs/Assets.xcassets/Colors/*.colorset').count.to_s"</automated>
  </verify>
  <done>10 colorset directories exist under Assets.xcassets/Colors/ with correct light/dark sRGB values. 3 Inter .otf files exist in BlossomHubs/ and are registered in project.pbxproj resources build phase. Info.plist contains UIAppFonts array. All verified by xcodeproj gem read-back.</done>
</task>

<task type="auto">
  <name>Task 2: BlossomFont enum and BlossomTheme refactor</name>
  <files>
    BlossomHubs/Core/Theme/BlossomFont.swift,
    BlossomHubs/Core/Theme/BlossomTheme.swift,
    BlossomHubs/BlossomHubs.xcodeproj/project.pbxproj
  </files>
  <action>
    **Step 1: Create BlossomFont.swift** at BlossomHubs/Core/Theme/BlossomFont.swift.

    Caseless enum (no cases) with static computed properties returning Font.custom():
    ```
    largeTitle: Inter-SemiBold, size 34
    title: Inter-SemiBold, size 28
    headline: Inter-SemiBold, size 17
    subhead: Inter-Medium, size 15
    body: Inter-Regular, size 17
    callout: Inter-Regular, size 16
    caption: Inter-Regular, size 12
    buttonLabel: Inter-Medium, size 16
    ```

    **Step 2: Refactor BlossomTheme.swift** to use Color("name") lookups instead of Color(hex:).

    Replace:
    - `Color(hex: "#35C7B2")` -> `Color("BlossomTeal")`
    - `Color(hex: "#7361F7")` -> `Color("BlossomViolet")`
    - `Color(hex: "#FF7833")` -> `Color("BlossomOrange")`
    - `Color(hex: "#1E222A")` -> `Color("BlossomDarkNavy")`
    - `Color(hex: "#565E76")` -> `Color("BlossomSlate")`

    Add new semantic token properties:
    - `static let background = Color("BlossomBackground")`
    - `static let cardSurface = Color("BlossomCardSurface")`
    - `static let cardBorder = Color("BlossomCardBorder")`
    - `static let primaryText = Color("BlossomPrimaryText")`
    - `static let secondaryText = Color("BlossomSecondaryText")`

    Keep the Color(hex:) extension (retain for utility — don't break anything that might use it). Keep tabActive, tabInactive, tabBarBackground as they are (systemBackground auto-adapts per user decision).

    **Step 3: Register BlossomFont.swift** in project.pbxproj using xcodeproj Ruby gem — add to Core/Theme group and target source build phase.

    **Step 4: Add debug font verification** to BlossomHubsApp.swift init(). Add inside `#if DEBUG`:
    ```swift
    assert(UIFont(name: "Inter-Regular", size: 17) != nil, "Inter-Regular font not loaded — check UIAppFonts in Info.plist")
    assert(UIFont(name: "Inter-Medium", size: 17) != nil, "Inter-Medium font not loaded")
    assert(UIFont(name: "Inter-SemiBold", size: 17) != nil, "Inter-SemiBold font not loaded")
    ```
    This catches silent font fallback at launch.

    **Step 5: Remove .preferredColorScheme(.light)** from BlossomHubsApp.swift body to enable dark mode testing (per RESEARCH.md open question 2 recommendation). Dark mode colorsets are meaningless if the app never shows dark UI.
  </action>
  <verify>
    <automated>cd /Users/nsimpson/Documents/GitHub/Blossom-Development && grep -c 'Color("Blossom' BlossomHubs/Core/Theme/BlossomTheme.swift && grep -c 'Inter-' BlossomHubs/Core/Theme/BlossomFont.swift && grep -c 'UIAppFonts' BlossomHubs/Info.plist && grep -c 'Inter-Regular' BlossomHubs/App/BlossomHubsApp.swift</automated>
  </verify>
  <done>BlossomTheme.swift uses Color("name") for all 10 color tokens (zero inline hex values for brand colors). BlossomFont.swift exists with 8 static Font.custom() properties. BlossomHubsApp has debug font assertions. .preferredColorScheme(.light) is removed. BlossomFont.swift registered in project.pbxproj.</done>
</task>

</tasks>

<verification>
- All 10 colorset Contents.json files have valid JSON with sRGB components in 0.0-1.0 range
- BlossomTheme.swift contains zero Color(hex:) calls for brand/semantic colors (Color(hex:) extension retained for utility)
- BlossomFont.swift references exactly 3 font names: Inter-Regular, Inter-Medium, Inter-SemiBold
- Info.plist contains UIAppFonts with 3 entries
- All new files registered in project.pbxproj (fonts in resources phase, BlossomFont.swift in source phase)
- .preferredColorScheme(.light) removed from BlossomHubsApp
</verification>

<success_criteria>
- Brand colors and semantic tokens defined as Asset Catalog color sets with correct light/dark variants
- Inter font files bundled, registered in Info.plist, and added to Copy Bundle Resources
- BlossomFont enum provides all 8 type scale levels via Font.custom()
- BlossomTheme refactored to use Color("name") lookups — no more inline hex for brand colors
- Debug assertions verify font loading at launch
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system-and-mock-data/02-01-SUMMARY.md`
</output>
