---
phase: 09-creator-earnings-and-demo-polish
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - BlossomHubs/Core/Components/SectionHeader.swift
  - BlossomHubs/Core/Components/BlossomButton.swift
  - BlossomHubs/Core/Components/BlossomCard.swift
  - BlossomHubs/Core/Components/AvatarView.swift
  - BlossomHubs/Core/Components/VerifiedBadge.swift
  - BlossomHubs/Core/Components/TagView.swift
  - BlossomHubs/Core/Components/EmptyStateView.swift
  - BlossomHubs/Core/Components/PlaceholderTabView.swift
  - BlossomHubs/Core/Components/LockedContentOverlay.swift
  - BlossomHubs/Features/TabBar/BlossomTabBar.swift
  - BlossomHubs/Features/Hubs/HubsTopNavBar.swift
  - BlossomHubs/Features/Hubs/Community/CommunityHubView.swift
  - BlossomHubs/Features/Hubs/Subscription/CancelRetentionSheet.swift
  - BlossomHubs/Features/Hubs/Forums/ForumReplyRow.swift
  - BlossomHubs/Features/Hubs/Forums/ForumThreadRow.swift
  - BlossomHubs/Features/Hubs/Forums/ForumThreadDetailView.swift
autonomous: false
requirements: [CRTR-06]

must_haves:
  truths:
    - "No .foregroundColor() calls remain in app source files (excluding ComponentsKit third-party)"
    - "Every screen in demo flow renders correctly in dark mode"
    - "Inter font renders at correct weight across demo flow screens"
    - "Complete demo flow runs without visible errors from discovery splash through earnings"
  artifacts:
    - path: "BlossomHubs/Core/Components/BlossomButton.swift"
      provides: "Migrated .foregroundStyle() calls"
      contains: "foregroundStyle"
    - path: "BlossomHubs/Features/Hubs/Forums/ForumThreadDetailView.swift"
      provides: "Migrated .foregroundStyle() calls"
      contains: "foregroundStyle"
  key_links:
    - from: "All 16 migrated files"
      to: "BlossomTheme color tokens"
      via: ".foregroundStyle() replacing .foregroundColor()"
      pattern: "\\.foregroundStyle\\("
---

<objective>
Migrate all deprecated .foregroundColor() calls to .foregroundStyle() across 16 app source files, then verify the complete demo flow in both light and dark mode with Inter font spot-check.

Purpose: Demo-readiness requires zero deprecated API warnings and correct visual rendering in both color schemes. The .foregroundColor -> .foregroundStyle migration is mechanical but touches the entire component library. The dark mode audit confirms no regressions. The checkpoint verifies the end-to-end demo flow.
Output: Clean codebase with modern API usage, verified demo flow.
</objective>

<execution_context>
@/Users/nsimpson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nsimpson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-creator-earnings-and-demo-polish/09-RESEARCH.md
@.planning/phases/09-creator-earnings-and-demo-polish/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1a: Migrate .foregroundColor() in Core/Components (9 files)</name>
  <files>
    BlossomHubs/Core/Components/SectionHeader.swift,
    BlossomHubs/Core/Components/BlossomButton.swift,
    BlossomHubs/Core/Components/BlossomCard.swift,
    BlossomHubs/Core/Components/AvatarView.swift,
    BlossomHubs/Core/Components/VerifiedBadge.swift,
    BlossomHubs/Core/Components/TagView.swift,
    BlossomHubs/Core/Components/EmptyStateView.swift,
    BlossomHubs/Core/Components/PlaceholderTabView.swift,
    BlossomHubs/Core/Components/LockedContentOverlay.swift
  </files>
  <action>
Find-and-replace `.foregroundColor(` with `.foregroundStyle(` in all 9 Core/Components files listed above. This is a mechanical 1:1 replacement — `.foregroundStyle()` accepts the same Color arguments.

**IMPORTANT exceptions — do NOT modify:**
- Any files inside `BlossomHubs/Build/SourcePackages/` (ComponentsKit third-party code)
- The `foregroundColor` property NAME in `TagView.swift` (line 11: `var foregroundColor: Color`) — this is a computed property name, not a modifier call. Only change the `.foregroundColor(fg)` modifier call on line 49 to `.foregroundStyle(fg)`.

**Specific file notes from RESEARCH.md dark mode audit:**
- `LockedContentOverlay.swift` lines 31, 35, 39: `.foregroundColor(.white)` -> `.foregroundStyle(.white)` — these are correct (white text on dark blurred overlay)

After replacements, run an intermediate build to confirm Core/Components compile cleanly before proceeding to Features files.
  </action>
  <verify>
    <automated>cd /Users/nsimpson/Documents/GitHub/Blossom-Development && xcodebuild -project BlossomHubs/BlossomHubs.xcodeproj -scheme BlossomHubs -destination 'platform=iOS Simulator,name=iPhone 16 Pro' build 2>&1 | tail -5</automated>
  </verify>
  <done>All .foregroundColor() modifier calls in Core/Components files replaced with .foregroundStyle(). TagView.foregroundColor property name preserved. Intermediate build succeeds.</done>
</task>

<task type="auto">
  <name>Task 1b: Migrate .foregroundColor() in Features and Hubs (7 files)</name>
  <files>
    BlossomHubs/Features/TabBar/BlossomTabBar.swift,
    BlossomHubs/Features/Hubs/HubsTopNavBar.swift,
    BlossomHubs/Features/Hubs/Community/CommunityHubView.swift,
    BlossomHubs/Features/Hubs/Subscription/CancelRetentionSheet.swift,
    BlossomHubs/Features/Hubs/Forums/ForumReplyRow.swift,
    BlossomHubs/Features/Hubs/Forums/ForumThreadRow.swift,
    BlossomHubs/Features/Hubs/Forums/ForumThreadDetailView.swift
  </files>
  <action>
Find-and-replace `.foregroundColor(` with `.foregroundStyle(` in all 7 Features/Hubs files listed above. This is a mechanical 1:1 replacement.

**Specific file notes from RESEARCH.md dark mode audit:**
- `CancelRetentionSheet.swift` line 65: `.foregroundColor(.white)` -> `.foregroundStyle(.white)` — verify this button sits on a colored (red/destructive) background. If it does, `.white` is correct. If the button could appear on a white card surface, flag it.
- `HubsTopNavBar.swift` line 35: `.foregroundColor(.white)` -> `.foregroundStyle(.white)` — correct (white "9+" on red capsule)
- `CommunityHubView.swift` line 121: `.foregroundColor(.white)` -> `.foregroundStyle(.white)` — correct (white "+" on violet FAB)

After all replacements, verify NO `.foregroundColor(` modifier calls remain in app source files (grep excluding Build/ directory).
  </action>
  <verify>
    <automated>cd /Users/nsimpson/Documents/GitHub/Blossom-Development && grep -r '\.foregroundColor(' BlossomHubs/ --include='*.swift' --exclude-dir='Build' | grep -v 'var foregroundColor' | wc -l | tr -d ' ' | xargs -I{} test {} -eq 0 && echo "PASS: No .foregroundColor() calls remain" || echo "FAIL: .foregroundColor() calls still exist"</automated>
  </verify>
  <done>All .foregroundColor() modifier calls in Features/Hubs files replaced with .foregroundStyle(). Full build succeeds. Zero deprecated foregroundColor modifier calls remain in app code.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Demo flow visual verification checkpoint</name>
  <action>
Present the complete demo flow verification checklist to the user for manual verification in iOS Simulator. This covers earnings visualization, dark mode audit, Inter font spot-check, and end-to-end demo flow.
  </action>
  <verify>User confirms all checklist items pass</verify>
  <done>User types "approved" confirming the complete demo flow is ready</done>
  <what-built>Complete Phase 9: Creator earnings chart with interactive bars, revenue breakdown, per-tier split, subscriber count display, animation polish (haptic feedback, numeric transitions), plus deprecated API migration across 16 files. The full Blossom Communities prototype is now demo-ready.</what-built>
  <how-to-verify>
    Run the app in Simulator and verify the complete demo flow:

    **Earnings verification (Creator Dashboard):**
    1. Open Hubs tab -> tap "Manage my Hub" card
    2. Verify earnings section appears between stat cards and section links
    3. Verify subscriber count is displayed in the earnings section
    4. Verify bar chart shows violet gradient bars
    5. Tap a bar — it should highlight, others dim, callout shows dollar amount, haptic feedback fires
    6. Tap same bar again — deselects
    7. Switch between 1M / 3M / 6M — chart updates, breakdown updates, subscriber count updates, haptic feedback fires
    8. Verify revenue amounts animate smoothly when switching periods (numeric text transition)
    9. Verify gross - 10% fee = net payout math
    10. Verify per-tier bars show colored progress (Observer=emerald, Investor=blue, Pro=violet)

    **Dark mode audit (toggle Appearance in Simulator: Features -> Toggle Appearance):**
    11. Hubs discovery screen — cards readable, no white-on-white
    12. Community preview page — text visible, tier sheet readable
    13. Community hub landing — banner, link-tree, section pager all correct
    14. Content feed — post cards, locked overlays correct
    15. Forums — thread list, replies, composer all readable
    16. Creator Dashboard + earnings — chart adapts, text readable

    **Inter font spot-check:**
    17. Compare headline text (semibold) vs body text (regular) on any screen — weights should visibly differ. If all text looks the same weight, Inter may have silently fallen back to SF Pro.

    **End-to-end demo flow:**
    18. Discovery splash -> browse -> tap community -> preview -> expand tier -> Subscribe -> payment sheet -> confetti -> community landing -> content feed -> tap locked post -> forum thread -> FAQ -> dashboard -> earnings
  </how-to-verify>
  <resume-signal>Type "approved" if demo flow passes, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `grep -r '.foregroundColor(' BlossomHubs/ --include='*.swift' --exclude-dir='Build' | grep -v 'var foregroundColor'` returns zero results
- Project builds without errors
- All screens render correctly in both light and dark mode
- Complete demo flow runs without crashes or visual artifacts
</verification>

<success_criteria>
- Zero deprecated .foregroundColor() modifier calls in app source code
- Dark mode audit passes on all demo flow screens
- Inter font renders with correct weight differentiation
- End-to-end demo flow completes without errors
- Human approves visual verification checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/09-creator-earnings-and-demo-polish/09-02-SUMMARY.md`
</output>
