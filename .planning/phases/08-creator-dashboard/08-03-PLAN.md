---
phase: 08-creator-dashboard
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - BlossomHubs/Features/Hubs/Creator/ComposePostView.swift
  - BlossomHubs/Features/Hubs/Discovery/CommunityCardView.swift
  - BlossomHubs/Features/Hubs/Forums/ForumThreadRow.swift
  - BlossomHubs/Features/Hubs/Forums/ForumReplyRow.swift
  - BlossomHubs/Features/Hubs/FAQ/FAQEntryRow.swift
autonomous: true
requirements:
  - CRTR-05
  - CRTR-07

must_haves:
  truths:
    - "Creator can compose a text post, trade highlight, or YouTube link post"
    - "Post type is selectable via segmented control at top of compose form"
    - "Collection assignment uses a dropdown picker of existing collection names"
    - "Tier gate assignment uses a dropdown picker of the community's tiers"
    - "Trade highlight ticker entry parses dollar-sign prefixed tickers into tags"
    - "Published post appears at top of content feed immediately"
    - "Verified badge is visible on creator profiles in discovery cards, forum posts, and FAQ entries"
  artifacts:
    - path: "BlossomHubs/Features/Hubs/Creator/ComposePostView.swift"
      provides: "Post creation form with type switcher, collection picker, tier gate picker"
      min_lines: 60
  key_links:
    - from: "BlossomHubs/Features/Hubs/Creator/ComposePostView.swift"
      to: "CommunityStore.addPost"
      via: "Post creation and insertion at index 0"
      pattern: "addPost"
    - from: "BlossomHubs/Features/Hubs/Creator/ComposePostView.swift"
      to: "parseTickers"
      via: "Dollar-sign regex for trade highlight tickers"
      pattern: "\\$[A-Za-z.]+"
---

<objective>
Build the content publishing form with post type switching, collection/tier-gate assignment, and ticker parsing. Audit and ensure the verified creator badge displays on all relevant surfaces throughout the app.

Purpose: Completes the creator's content management capability and ensures creator identity is visually consistent across all subscriber-facing surfaces.
Output: ComposePostView with full publishing flow, verified badge visibility confirmed across discovery, forums, and FAQ.
</objective>

<execution_context>
@/Users/nsimpson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nsimpson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-creator-dashboard/08-CONTEXT.md
@.planning/phases/08-creator-dashboard/08-RESEARCH.md
@.planning/phases/08-creator-dashboard/08-01-SUMMARY.md

@BlossomHubs/Models/Community.swift
@BlossomHubs/Models/CommunityStore.swift
@BlossomHubs/Features/Hubs/Creator/CreatorDashboardView.swift
@BlossomHubs/Features/Hubs/Feed/PostCardView.swift
@BlossomHubs/Features/Hubs/Discovery/CommunityCardView.swift
@BlossomHubs/Features/Hubs/Forums/ForumComposeSheet.swift
@BlossomHubs/Shared/AvatarView.swift

<interfaces>
<!-- From Plan 01 — mutation helpers -->
From BlossomHubs/Models/CommunityStore.swift (after Plan 01):
```swift
func addPost(to communityID: UUID, post: Post)
func updateCommunity(id: UUID, update: (inout Community) -> Void)
static let wealthmaticaID: UUID
```

From BlossomHubs/Models/Community.swift:
```swift
enum PostType: String, Codable, CaseIterable {
    case text
    case tradeHighlight
    case youtubeLink
}

struct Post: Identifiable {
    let id: UUID
    var authorId: UUID
    var content: String
    var postType: PostType
    var stockTickers: [String]
    var youtubeURL: String?
    var requiredTierIndex: Int
    var publishedAt: Date
    var collection: String?
}
```

From BlossomHubs/Shared/AvatarView.swift:
```swift
struct AvatarView: View {
    let imageName: String
    var size: CGFloat = 40
    var showVerifiedBadge: Bool = false
    var showBadge: Bool = false
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Content publishing compose form</name>
  <files>
    BlossomHubs/Features/Hubs/Creator/ComposePostView.swift
  </files>
  <action>
    Replace the placeholder. Build a full post creation form with post type switching.

    **ComposePostView.swift** layout:
    - `.navigationTitle("Publish Content")` inline
    - `@Environment(CommunityStore.self)` and `@Environment(SubscriptionStore.self)`
    - Retrieve community from store using `creatorCommunityID`
    - @State properties: `selectedPostType: PostType = .text`, `content: String = ""`, `tickerInput: String = ""`, `youtubeURL: String = ""`, `selectedCollection: String?`, `selectedTierIndex: Int = 0`, `showSuccess: Bool = false`
    - Form layout:
      - Section "Post Type": Picker with `.pickerStyle(.segmented)` cycling through PostType.allCases. Display names: "Text", "Trade", "YouTube".
      - Section "Content": TextEditor or TextField(axis: .vertical) for post body content, lineLimit 5...12
      - Conditional section based on selectedPostType:
        - `.tradeHighlight`: TextField for ticker input with placeholder "$AMD, $TSLA" and helper caption "Separate tickers with commas"
        - `.youtubeLink`: TextField for YouTube URL with placeholder "https://youtube.com/watch?v=..." and `.keyboardType(.URL)`
      - Section "Organization":
        - Picker "Collection" with existing collection names extracted from community.posts (unique non-nil collection values). Include a "None" option.
        - Picker "Minimum Tier" with community.tiers enumerated, showing tier name and price. Use tier index as the value.
      - Section with "Publish" button: BlossomButton-style, teal accent, disabled when content is empty. On tap:
        1. Parse tickers from tickerInput using regex `\$[A-Za-z.]+` (only for tradeHighlight)
        2. Create a Post with: `authorId: community.creator.id`, `content: content`, `postType: selectedPostType`, `stockTickers: parsedTickers`, `youtubeURL: (youtubeLink only)`, `requiredTierIndex: selectedTierIndex`, `publishedAt: .now`, `collection: selectedCollection`
        3. Call `store.addPost(to: community.id, post: newPost)`
        4. Set showSuccess = true, then reset form fields after a brief delay
    - Add a `parseTickers(from:)` private function using Swift Regex: `let pattern = /\$[A-Za-z.]+/` and `input.matches(of: pattern).map { String($0.output) }`
    - Show a brief success feedback: either a checkmark overlay or an alert saying "Post published!"
  </action>
  <verify>
    <automated>xcodebuild build -project BlossomHubs/BlossomHubs.xcodeproj -scheme BlossomHubs -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -derivedDataPath BlossomHubs/Build 2>&1 | tail -5</automated>
  </verify>
  <done>ComposePostView renders with segmented post type picker, content field, conditional ticker/URL fields, collection picker, tier gate picker, and publish button. Publishing creates a Post and inserts it at the top of the community's posts array via CommunityStore.addPost. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Verified badge audit across all surfaces</name>
  <files>
    BlossomHubs/Features/Hubs/Discovery/CommunityCardView.swift,
    BlossomHubs/Features/Hubs/Discovery/CommunityHeroCardView.swift,
    BlossomHubs/Features/Hubs/Forums/ForumThreadRow.swift,
    BlossomHubs/Features/Hubs/FAQ/FAQEntryRow.swift
  </files>
  <action>
    Audit every surface where creator/ambassador profiles appear and ensure the verified badge (VerifiedBadge or AvatarView.showVerifiedBadge) is visible. The badge components already exist from Phase 2 — this task ensures they are USED everywhere.

    Surfaces to audit and fix if needed:

    1. **CommunityCardView.swift** — Check that AvatarView for the community creator uses `showVerifiedBadge: community.creator.isVerified`. If not present, add it. The card already shows the creator avatar; ensure the badge overlay appears.

    2. **CommunityHeroCardView.swift** — Same check: AvatarView should show verified badge for the creator.

    3. **ForumThreadRow.swift** (or equivalent forum row view) — Check if creator/ambassador posts show a verified badge or distinct visual treatment next to the author name. If the existing Phase 7 implementation already handles this via `isCreator`/`isAmbassador` booleans on ForumReply with highlighted background, confirm it works. If ForumThread list rows (not replies) show author info, ensure verified badge appears for creator-authored threads.

    4. **FAQEntryRow.swift** (or equivalent FAQ row view) — Check that answered FAQ entries where `answeredBy` is the creator show the verified badge or creator-distinguished styling. Phase 7 already added teal tint for creator answers — confirm this is working.

    5. If any surface is missing the badge, add `showVerifiedBadge: true` to the relevant AvatarView, or add a small VerifiedBadge() inline next to the creator name. Follow existing Phase 7 patterns (teal tint opacity(0.08) for creator backgrounds, verified checkmark badge).

    Note: This is primarily an audit task. If Phase 7 already implemented creator visual treatment comprehensively, this task may require minimal or no code changes. Document findings either way.
  </action>
  <verify>
    <automated>xcodebuild build -project BlossomHubs/BlossomHubs.xcodeproj -scheme BlossomHubs -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -derivedDataPath BlossomHubs/Build 2>&1 | tail -5</automated>
  </verify>
  <done>Verified badge appears on creator/ambassador profiles in: discovery cards (CommunityCardView, CommunityHeroCardView), forum posts (thread rows and reply rows), and FAQ entries (answered-by attribution). Build succeeds.</done>
</task>

</tasks>

<verification>
1. Build succeeds with zero errors
2. ComposePostView allows creating all 3 post types
3. Ticker parsing handles $AMD, $TSLA, $RY.TO, $BRK.B correctly
4. Published posts appear at top of content feed
5. Verified badge visible on all creator profile surfaces
</verification>

<success_criteria>
- Creator can publish text, trade highlight, and YouTube link posts with collection and tier-gate assignment
- Published posts appear immediately in the content feed with correct gating
- Verified creator badge displays on discovery cards, forum posts, and FAQ entries
- Build compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-creator-dashboard/08-03-SUMMARY.md`
</output>
